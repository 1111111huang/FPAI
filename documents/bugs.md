# FPAI Bug Tracker

This document tracks bugs and issues discovered during development and testing. Default status is `open`.

| Bug ID | Title | Description | Status | Notes |
| :--- | :--- | :--- | :--- | :--- |
| BUG-056 | Both configured Odds API keys (`ODDS_API_KEY`/`ODDS_API_KEY_2`) rejected with 401 Unauthorized for at least Serie A/Ligue 1 | Found live 2026-08-30 via user-shared Railway deploy logs, while investigating why matches from 2026-08-29 weren't auto-settling: `FallbackOddsClient.get_odds: key #1 failed (401 Client Error: Unauthorized ...)` immediately followed by an identical `key #2 failed (401 ...)` for `soccer_italy_serie_a` and `soccer_france_ligue_one`. A 401 (not 429) means the keys themselves are invalid/revoked, not just rate-limited/exhausted — a genuinely different failure class from the Tavily quota exhaustion fixed the same day ([[A86]]/[[A87]]). Traced via `superpowers:systematic-debugging`: does NOT explain the separate settlement gap this investigation started from — `FallbackOddsClient` is only used by the EOD job (new-recommendation generation), a completely separate scheduled job from the lessons/settlement job (`resolve_pending_recommendations`, which never touches odds data at all) — and `_try_each_client` doesn't retry/loop on a 401, so no hang risk to other background work. Per its own documented fallback contract the EOD job doesn't crash on this, it silently keeps last-known odds — meaning new recommendations for the affected leagues have been generated with stale odds since whenever these keys stopped working. | open | Mitigated 2026-08-30: added a third fallback key slot (`ODDS_API_KEY_3`, see [[W188]]/`app/backend/scheduler_wiring.py`'s `build_odds_client`) so a real, working third key can be configured in production. Left `open`, not `fixed` — the mitigation adds capacity, it doesn't diagnose *why* both existing keys started returning 401 (revoked? wrong plan tier? never valid for these two leagues specifically?), which still needs a direct check against The Odds API dashboard for both keys. |
| BUG-053 | UB (suggested stake) column disappears once a match completes, unlike the sibling Edge column | Reported live 2026-08-28 by direct user observation: a finished match's card shows no `Stake`/`UB` value at all. Root-caused to `app/frontend/components/MatchUI.tsx:1096`: the Stake column is gated `{!isCompleted && match.unitBetMultiplier != null && (...)}` — a deliberate guard (A82/W169), with its own comment reasoning "Only meaningful pre-match -- a completed match has nothing left to size a stake for." Inconsistent with the immediately adjacent Edge column (`:1056-1087`), which does the opposite for the identical completed-match case: it stays visible, switches to plain (non-green) styling, and gets a `Pre-match edge` badge (`:1074-1079`) rather than being hidden -- explicitly reasoned there as "This is a historical fact now." `unitBetMultiplier` is computed the same way (`rec.unit_bet_multiplier`, `:189`) and is available on a completed match's own cached recommendation exactly like `valueEdge` is, so there's no data-availability reason for the asymmetry -- it reads as an inconsistency between two sibling columns, not a deliberate product distinction. | fixed | Fixed 2026-08-28 alongside [[W186]] (`documents/app_user_stories.md`, PHASE 42): the `!isCompleted` guard was removed, exactly the fix predicted above — Stake stays visible post-match with a `Pre-match stake` badge. Bundled with a second, related user request (money-won replacing the Odds/Result box) in the same change. |
| BUG-055 | A completed `no_bet` match still showed a green "Hit" badge | Reported live 2026-08-29 by direct user screenshot ([[BUG-054]]'s own Liverpool v Nottingham draw, once the match finished): the top-right badge read "FT · HIT" and the Pick row echoed a green "Hit" tag under "Draw", despite the footer correctly stating "Was a No Bet pick". Root cause: `MatchCard`'s `hit` computation (`app/frontend/components/MatchUI.tsx`) resolved `bestMarket(match)`'s selection against the actual result via `marketCorrect()` whenever a match was completed and had *any* recommendation, without checking whether that shown market's own `recommendationType` was `no_bet` -- `bestMarket()` still returns the "least-bad no_bet" market as a display fallback when nothing in the recommendation was actionable (its own documented behavior), and that fallback market's selection coincidentally matching the real result read as a genuine "Hit" on a bet that was never actually recommended. | fixed | Added `shown.recommendationType !== "no_bet"` to the `hit` computation's guard -- one shared `const hit`, so both the top-right `HitBadge` and the inline Pick-row echo (both driven by the same variable) stop showing for a `no_bet` match in the same change. New test in `MatchUI.hitMiss.test.tsx`: a completed `no_bet` market whose selection matches the actual result shows neither "Hit" nor "Not Hit". Full frontend suite: 206/206 passed, `tsc --noEmit` clean. |
| BUG-054 | `config/prompts/agent_v1*.txt` hardcoded the value-edge/odds thresholds as a second, stale copy of `AgentConfig`'s real enforced values — drifted out of sync when production's thresholds were tightened | Reported live 2026-08-29 by direct user screenshot: a live match's card badge showed "No Bet" on a `result_3way`/draw pick, while its own "Full analysis" reasoning argued the opposite — "+13.4% value edge that clears the 5% threshold", "Draw odds of 5.0 fall inside the 1.2-11.0 direct-bet range, so the price is actionable now" — and the match went on to finish as a draw (the pick would have hit). The full reasoning trace showed the actual downgrade chain: `Downgraded 'result_3way' from direct_bet to conditional: current_odds 5.0 outside [1.71, 4.0].` then `Downgraded 'result_3way'/'draw' from conditional to no_bet: 'conditional' only applies to over/yes-type markets...`. Root cause: [1.71, 4.0] is `config/agent_config.yaml`'s real, live production `min_odds_threshold`/`max_odds_threshold` (tightened from [1.2, 11.0] by direct user request on 2026-08-28, per that file's own comment) — but `config/prompts/agent_v1.txt` (`_load_system_prompt`, `src/agent/graph.py`) still told the LLM the old "1.2 or above 11.0" bounds verbatim, a plain `path.read_text()` with no connection to `AgentConfig` at all. The LLM correctly called the market `direct_bet` by the (stale) rule it was actually given, `_downgrade_direct_bet_outside_odds_bounds` (A29, `src/agent/schema.py`) then correctly downgraded it using the *real* config to `conditional`, and `_restrict_conditional_to_eligible_markets` (A54) correctly downgraded that to `no_bet` since `result_3way` is never conditional-eligible — every code-enforced rule fired exactly as documented, but the LLM's own `explanation` prose (written under the stale bounds) still argued for a bet the app had to refuse, reading as a self-contradictory card. Same latent drift affected `min_conditional_odds_threshold` (prompt said 1.5, real value 1.71) and the new `max_conditional_odds_threshold` ceiling (added 2026-08-28, never mentioned in the prompt at all) — and applied identically to all 4 prompt files (`agent_v1[_aggressive\|_balanced\|_conservative].txt`), which share the same hardcoded lines. | fixed | `_load_system_prompt()` now substitutes `{{MIN_VALUE_EDGE}}`/`{{MIN_ODDS_THRESHOLD}}`/`{{MAX_ODDS_THRESHOLD}}`/`{{MIN_CONDITIONAL_ODDS_THRESHOLD}}`/`{{MAX_CONDITIONAL_ODDS_CLAUSE}}` placeholders (added to all 4 prompt files, replacing the hardcoded numbers) from the real `AgentConfig` at load time — one source of truth instead of two that can independently drift, for every posture/config, not just production's. The conditional-ceiling clause is templated in only when `max_conditional_odds_threshold` is finite (omitted entirely for every config that leaves it unbounded, preserving prior text for those). New `tests/test_agent_prompt_thresholds.py` (4 tests): prompt reflects a tightened odds range and rejects the old hardcoded one, reflects a tuned value-edge/conditional-floor, only mentions the ceiling clause when the config sets one, and no `{{...}}` placeholder is left unsubstituted in any of the 4 posture prompts. Full suite: 1354 passed / 1 skipped (5 pre-existing, unrelated failures in `test_fixtures_endpoint.py`, confirmed present on `main` before this change via `git stash`). **Not separately re-verified live against production** (no access to the deployed instance's own cache) — verified via direct `_load_system_prompt(AgentConfig.default())` call that the real production config now renders "odds below 1.71 or above 4" into the prompt. |
| BUG-052 | T-30 refresh reintroduced [[W164]]'s "most picks are draws" bug — `t30_refresh.py` never gained `total_goals`/`btts` odds, only `eod_batch.py` did | Reported live 2026-08-27 by direct user observation: recommendations were spread across markets right after EOD generation, but the two most-recently-kicked-off matches (i.e. the ones that had gone through a T-30 refresh) had both flipped to `result_3way`/draw. Root-caused via `git log`: [[W164]] (2026-08-25) fixed EOD generation's "20/24 live picks were draws" bug by fetching `total_goals`/`btts` odds via `OddsAPIClient.get_event_odds()` and threading them into the agent's prompt — but that change only touched `eod_batch.py`'s `_generate_one`; `refresh_match_at_t30` (`t30_refresh.py`, last touched at W162, before W164 existed) still built `match_info` with 1X2 odds only. The agent's own prompt rule ("if you don't have a real current price for a market, don't recommend it at all" — `config/prompts/agent_v1.txt`) then structurally limited every T-30 refresh to `result_3way`, and the already-documented "draw-framing fallacy" lesson (agents citing "evenly matched" framing as evidence for draw) pushed most of those toward draw specifically — reproducing W164's exact original symptom every time a match crossed its T-30 mark. Confirmed via `test_t30_refresh.py` having zero references to `total_goals`/`btts` before this fix. | fixed | Extracted the fetch-or-reuse-from-cache secondary-odds logic (W164/W164a) out of `eod_batch.py`'s `_generate_one` into a new shared `add_secondary_odds()` function, called by both `run_eod_batch` and `refresh_match_at_t30` — one implementation instead of two that can silently drift out of sync again, which is exactly how this regressed the first time. `t30_refresh.py` now calls it before its `already_fresh()` dedup check, so a secondary-market-only price move still triggers a real refresh (mirroring EOD's own ordering). 3 new tests in `test_t30_refresh.py` (written first, confirmed failing pre-fix): totals/btts fetched and threaded into both the prompt and the cache dedup key, reused from cache without a fresh API call when h2h odds are unchanged, and left untouched when the odds client lacks `get_event_odds` (sandbox/backtest). Full suite: 1337 passed / 1 skipped, zero regressions. |
| BUG-050 | football-data.org's `LIVE` match status wasn't recognized by the fixtures status filter | Found live 2026-08-16 — a real in-progress match reported `status="LIVE"` verbatim, not `IN_PLAY` as [[W144]]'s original live-match fix assumed; confirmed via a direct single-match fetch (the list endpoint's own `status=IN_PLAY,PAUSED` filter returned zero results for a match whose own status field was `LIVE`). Same invisibility shape as [[BUG-048]]/[[BUG-049]] — a filter silently drops any status value it doesn't name, rather than erroring. | fixed | `get_fixtures()`'s status filter extended to `SCHEDULED,TIMED,IN_PLAY,PAUSED,LIVE` ([[W148]]). 1 new regression test plus 2 existing tests updated. Full `app/backend/tests/test_football_data_client.py`: 16 passed. |
| BUG-051 | `MatchUI.dateboundary.test.tsx`'s long-documented "pre-existing flake" (first noted W61, reconfirmed unrelated a dozen+ times since without ever being root-caused) was a real transient double-render in `MatchExplorerPage`, not a CI timing artifact | Investigated live 2026-08-25 after the flake resurfaced during an unrelated frontend change. `useSandboxAsOf()` (`lib/useSandboxAsOf.ts`) returns a real `new Date()` guess on first render, then a *second*, distinct `Date` object once `getSandboxStatus()` resolves and reports `sandbox_mode`. `MatchExplorerPage`'s fixture-fetch effect depends on `[asOf, retryTick]`, so that reference change fires a second effect run whose `load()` unconditionally calls `setMatches(null)` again before re-fetching — a real, visible reset back to the loading skeleton on every sandbox-mode mount. Harmless in real usage (`sandbox_mode` is never true for a production user — see the hook's own doc comment) and normally invisible even in tests (both fetches resolve on the same microtask tick via `mockResolvedValue`), but a plain `findByText(...)` followed by a *synchronous* `getByText(...)` two lines later could land exactly in that transient null-reset window and fail. A first fix attempt (raising `findByText`'s timeout) was tried and directly disproven via timing instrumentation — the failing assertion completed in well under 100ms every time, nowhere near any timeout, confirming the check wasn't slow, it was synchronously sampling a real mid-flight state. | fixed | Test-side fix only (no production code changed — the double-fetch is real but confined to sandbox/dev-testing mode, never reachable in production): every assertion in the 4 affected tests that could observe the transient state is now inside one `waitFor(...)` block instead of `findByText(...)` plus separate synchronous checks, so a pass only reports once the page has genuinely converged on its final state. Verified via 29 consecutive full-suite runs, 0 failures (previously ~1/3 of runs failed). `tsc --noEmit` clean, `npm run build` clean. |
| BUG-049 | Dashboard's fixture filter silently excluded every live match, not just completed ones | Found live 2026-08-14 while building [[W145]] (in-place completed-match display) — `DashboardPage`'s post-`fixtureToMatch()` filter was a `.filter((m) => m.status === "upcoming")` allowlist, written back when `Match["status"]` was a strict `"upcoming" \| "completed"` binary ([[BUG-046]]/[[BUG-047]]'s own era). W144 ([[W144]]) had already added a third `"live"` status, but nothing had revisited this filter — an allowlist silently drops any value it doesn't know about, so every currently-playing match vanished from the Dashboard the moment it kicked off, reappearing only once `FINISHED`. Not caught by W144's own tests, since none of them exercised the Dashboard's list-filtering path specifically, only `fixtureToMatch()`'s status derivation in isolation. | fixed | Replaced the allowlist with an explicit denylist scoped to the actual intent ("hide old completed matches, not today's"): `.filter((m) => m.status !== "completed" \|\| dayDiff(m.kickoffIso, asOf, sandboxMode) === 0)` — a live match (any day) and a completed match dated today both pass; only a completed match from a prior day is excluded. New regression test in `MatchUI.dashboardWindow.test.tsx` mixes a live fixture into the fetch response and confirms it renders (not silently dropped) alongside upcoming and today's-completed fixtures. Full frontend suite: 156/158 passed (the 2 remaining are `MatchUI.dateboundary.test.tsx`'s own pre-existing, already-confirmed-unrelated flake), `tsc --noEmit` clean. |
| BUG-048 | La Liga (SP1) fixtures silently missing from `/api/fixtures` for their entire next ~4 weeks | Found live 2026-08-14 via user screenshot showing the Dashboard's SP1 section empty despite real upcoming La Liga fixtures existing. `FootballDataClient.get_fixtures()` filtered upstream matches by `status=SCHEDULED` only. football-data.org marks near-term matches with a confirmed kickoff time as `TIMED`, not `SCHEDULED` — and, verified directly against the real API, honors that filter inconsistently per competition: for PL (E0) it returned both `TIMED` and `SCHEDULED` matches (not actually filtering), but for PD (La Liga) it strictly excluded every `TIMED` match, silently dropping SP1's entire next ~4 weeks (2026-08-15 → 2026-09-12, 40 matches) while EPL's identically-shaped near-term matches happened to survive purely by luck of the upstream inconsistency. | fixed | `status=SCHEDULED,TIMED` (comma-separated), confirmed live against the real API to reliably return the full fixture calendar for both competitions. 2 new tests (`app/backend/tests/test_football_data_client.py`): the outgoing `status` param includes both values; a `TIMED`-status match normalizes correctly (unrelated to which values were requested). Full suite: 1004 passed / 1 skipped, zero regressions. Superseded in scope (not reverted) by [[W144]] the same week, which further extended the same filter to `SCHEDULED,TIMED,IN_PLAY,PAUSED` for live-match visibility. |
| BUG-045 | Boot-time pregenerate (W103) OOM-crashed the deployed instance in an infinite loop | Found live 2026-08-13 via user-shared Railway deploy logs + metrics + crash-notification email ("Deploy Ran Out Of Memory!"). Initial read of the logs alone (repeated `Started server process` → `Unmapped team ...` warnings → `Mounting volume` cycles, no Python traceback anywhere) suggested a health-check-driven restart; the Metrics tab's memory graph (peak 1.91GB against a 4GB limit) seemed to rule out OOM entirely — but Railway's own crash-notification email confirmed OOM directly, a case where the periodically-*sampled* usage graph missed a real, brief burst above the limit between samples that the container's own OOM killer (which watches continuously, not on a sample interval) still caught. Root cause: `lifespan()`'s startup hook fires `_pregenerate_recommendations()` (W103, shipped the same session) on every boot when `ENABLE_SCHEDULER=1` (persists as a Railway env var, so on for every future redeploy) — it walks every competition (E0/SWE/SP1) sequentially, and each `eod_batch.run_eod_batch(..., concurrency=5)` call stacks up to 5 concurrent `asyncio.to_thread()` agent calls (each loading its own `ForecastService` models/feature frames) directly on top of the process's own fresh, not-yet-settled import-time memory — a burst the scheduled nightly EOD job (same `concurrency=5` default, but running hours into a long-stable process) never has to survive. Because pregenerate re-fires on every boot, a single OOM became a genuine infinite crash loop: crash → Railway restarts → pregenerate fires again immediately → crash again, never completing one full pass. | fixed | New `_PREGENERATE_DEFAULT_CONCURRENCY = 2` (`app/backend/main.py`), deliberately lower than `eod_batch.run_eod_batch()`'s own default of 5 (a value already proven safe for the *nightly* path, not narrowed) — threaded through `_pregenerate_recommendations()`'s new `concurrency` parameter to its `run_eod_batch(...)` call. Both real callers (the boot hook, the admin trigger endpoint) invoke `_pregenerate_recommendations()` with keyword args and don't pass `concurrency` explicitly, so both pick up the safer default automatically; the scheduled nightly EOD job (`scheduler_wiring.py`) is untouched — no evidence it's affected, and it wasn't the confirmed failure. New test `test_bug_045_defaults_to_a_lower_concurrency_than_eod_batchs_own_default` (`app/backend/tests/test_pregenerate_recommendations.py`). Red-first verified: temporarily removed the `concurrency=concurrency` kwarg from the `run_eod_batch()` call, confirmed the new test fails with a real `KeyError: 'concurrency'`, reverted. Full backend suite: 309 passed, zero regressions. **Not separately confirmed**: the exact peak memory figure at the moment of the OOM kill (Railway's dashboard doesn't expose that, only the periodic samples) — the fix targets the identified structural cause (concurrent thread-pool agent calls stacking on fresh process memory), not a number reproduced locally. |
| BUG-046 | Dashboard's sticky "Daily Edges" title let scrolled match cards bleed through above it | Reported live 2026-08-14 via user screenshot: on desktop, after scrolling the match list, a date-group panel header ("Sun, Aug 16") rendered overlapping/above the sticky "Daily Edges" title instead of staying cleanly hidden behind it. Root-caused live with Playwright (dev server + mocked `/fixtures`/`/sandbox` routes + real scroll + computed-style inspection, not guesswork): `<main>` (`AppShell.tsx`) is the `lg:overflow-y-auto` scroll container for desktop *and* carried its own `pt-8` top padding directly. Padding on a scroll container sits *inside* its scrollport, so scrolled children (the match cards, in normal flow) can slide up through that 32px strip during scroll -- it isn't reserved dead space, it's part of the scrollable area. The sticky title's own box (`lg:sticky lg:top-0 lg:z-10 lg:bg-page`) only covers itself, starting 32px below the scrollport's true top edge (confirmed via `getBoundingClientRect()`: title top = 95px while `<main>`'s own top = 63px, i.e. main's `pt-8` exactly explained the gap) -- so that 32px gap above the title had nothing opaque covering it, letting scrolled cards show through. | fixed | Moved `pt-8` off `<main>` itself onto a new plain wrapper div rendered *inside* `<main>`, around `{children}` (`AppShell.tsx`). That wrapper isn't the scroll container, so its padding is now ordinary in-flow spacing that scrolls away with the rest of the page content instead of staying exposed inside the scrollport -- `<main>`'s true top edge now has zero padding, so any page's `lg:sticky lg:top-0` header lands flush against it with no exposed gap. Re-verified the exact same live Playwright repro after the fix: sticky title's `rect.top` now equals `<main>`'s own `rect.top` (63px = 63px) and the scrolled screenshot shows the title cleanly covering the cards scrolled beneath it, with the unscrolled state and the real-device mobile layout both visually unchanged (mobile never used `<main>` as its own scroll container, so was never at risk). New regression test in `AppShell.test.tsx` asserts `<main>` itself never carries a `pt-8` class and that the padding lives on its first child instead -- the structural invariant the bug depended on (jsdom can't exercise real sticky/scroll/paint behavior, so this pins the fix's mechanism rather than the visual symptom). Full frontend suite: 130/130 passed (plus the one known pre-existing flake in `MatchUI.dateboundary.test.tsx`, confirmed unrelated -- passes clean in isolation). |
| BUG-047 | Sticky "Daily Edges" title/rail traveled ~32px before locking in place, reading as a laggy snap | Reported live 2026-08-14 via direct user feedback ("still goes up by a little bit before turning stationary. Also the scrolling feels laggy") on the very next screenshot after [[BUG-046]] shipped. Root cause: BUG-046's fix moved `<main>`'s `pt-8` onto a wrapper *inside* `<main>` so it would scroll away instead of leaking through the sticky title's gap -- but that traded away a property the *broken* version had for free: padding directly on the scroll container itself doesn't count as scrollable travel for `position: sticky`'s `top: 0` threshold (the container's own padding is already "spent" toward that offset), while padding on a normal in-flow descendant does. So post-BUG-046, the title's natural (unscrolled) position sat 32px below `<main>`'s true top, and it only reached its stuck position after the user scrolled past that 32px -- a real, user-visible "settle" jump each time, easily read as lag. | fixed | Verified live with Playwright (same dev-server + mocked-fixtures approach) that the natural/unscrolled sticky title and rail both sat 32px below `<main>`'s own top, and that this gap was consumed as real scroll travel before "sticking" (confirmed by sampling `getBoundingClientRect()` at 1px, 5px, 15px, 32px of scroll -- title didn't reach `<main>`'s top until 32px). Fix: added `lg:-mt-8` to `DashboardPage`'s outer content row (`MatchUI.tsx`) to cancel the wrapper's `pt-8`, landing the row's natural position flush with `<main>`'s true top with zero travel required; added `lg:pt-8` back onto each sticky child individually (title and rail) so their own opaque backgrounds still cover that reclaimed space (keeping [[BUG-046]]'s fix intact) and their content keeps the same visual breathing room. Re-verified live: sticky title's `top` now reads `<main>`'s own top (63px) at every sampled scroll offset from 1px through 100px, unscrolled *and* scrolled screenshots both match pre-bug spacing/coverage. Separately checked for a genuine performance issue (not just the visual snap) via a CDP performance trace of a real wheel-scroll interaction -- near-zero script execution and task duration, no evidence of layout thrashing -- so the "laggy" report is most likely this same catch-up jump, not a distinct perf bug; flagged to the user to recheck live now that the jump itself is gone. Full frontend suite: 130/130 passed (the one known pre-existing flake in `MatchUI.dateboundary.test.tsx` reconfirmed unrelated). |
| BUG-044 | `TeamNameMapper.map_team()`'s exact-string lookup is diacritic-sensitive, so accented club names ("Atlético Madrid", "Deportivo La Coruña") the Odds API returns for SP1 silently miss `config/team_mapping.json`'s already-correct-but-unaccented entries ("Atletico Madrid", "Deportivo La Coruna") | Found live 2026-08-13 via user-shared production log screenshot: repeated `Unmapped team 'Atlético Madrid'`/`'Deportivo La Coruña'` warnings alongside `'Santander'`/`'Real Racing Club de Santander'` (the latter two already a documented, genuine cold-start per `app/backend/tests/test_la_liga_football_data_team_mapping.py::test_santander_is_a_genuine_cold_start_not_a_mapping_gap` — a newly-promoted club with zero `raw_matches` history to map to at all). The other two are a different class entirely: both clubs already have correctly-mapped entries in `config/team_mapping.json` ("Atletico Madrid"/"Atletico de Madrid" → "Ath Madrid", "Deportivo La Coruna"/"Deportivo de La Coruna" → "La Coruna") — added by two earlier, source-specific audits (W59 for Sweden's Odds-API full names, W78 for La Liga's football-data.org `shortName` field, e.g. "Atleti"/"Deportivo") — but SP1's own Odds-API feed (`odds_sport_keys.py`'s `soccer_spain_la_liga`, the same provider W59 already found returns Sweden's full accented names) returns La Liga's full *official* names with diacritics, a third spelling variant neither earlier audit covered. `map_team()`'s lookup (`src/ingestion/common/team_mapping.py`) was a plain `normalized in self.mapping` exact match with no accent-folding, so "Atlético Madrid" (é) can never match the "Atletico Madrid" (no é) key no matter how many manually-enumerated variants exist. | fixed | Root-caused, not patched a third time: rather than a third manual audit enumerating The Odds API's SP1 spellings (mirroring W59/W78), added a stdlib-only `_fold_accents()` (NFKD-decompose + drop combining marks — no new dependency) and a `self._folded_mapping` index built once at load time; `map_team()` tries the fold on both the incoming name and the mapping's own keys as a fallback after the exact match misses, before falling through to the fuzzy/candidates path. Folds both directions (an accented key still matches a plain-ASCII input, and vice versa), so it also closes this class of gap for any future competition's accented names, not just SP1's already-known two. Deliberately doesn't touch the fuzzy `suggest()` path or `Santander`'s genuine cold-start behavior — verified live post-fix: `Atlético Madrid`/`Deportivo La Coruña` now resolve silently to `Ath Madrid`/`La Coruna`; `Santander`/`Real Racing Club de Santander` still correctly warn (real gap, not an accent issue, folding doesn't change either string). New tests: 3 in `tests/test_understat.py` (accent-insensitive on the input side, on the mapping side, and a genuinely-unmapped accented name still reports unmapped rather than being papered over), 1 in `app/backend/tests/test_la_liga_football_data_team_mapping.py` against the real production mapping file. Full suite: 1001 passed / 1 skipped, zero regressions (up from 997). |
| BUG-043 | Every Allsvenskan (SWE) fixture shows "Cold start — thin history" regardless of season stage — `feature_completeness` structurally capped at ~71.6%, not a data-freshness or join problem | Found live 2026-08-13 via user screenshot: two mid-season SWE cards (IF Elfsborg v Västerås SK; Mjällby AIF v IK Sirius) both showed the cold-start badge despite both teams having 15+ real matches each in `raw_matches` this season, correct team-name mapping (`config/team_mapping.json`), and no snapshot-staleness signature (ruled out BUG-036's known failure mode — these were W103's first-ever pregeneration for these fixtures, not a stale replay). Root-caused with real numbers, not guessed: SWE's post-`enabled_feature_groups`-gating feature set is 74 columns (`src/logic/competition_registry.py`); 9 are `MKT_*` Asian-handicap/over-under columns, a known, documented, permanent gap (SWE's football-data.co.uk feed only carries closing 1X2 odds — `src/logic/feature_groups.py`'s own docstring). The other 12 — `OFF_*_XG_R3/R5`, `DEF_*_XGA_R3/R5`, `OFF_*_LUCK_R3/R5` — were an *undocumented* permanent gap: SWE's feed has zero `xg_h`/`xg_a` coverage (confirmed live: 0/3489 SWE rows in `raw_matches`), and Understat, the xG backfill source `E0`/`SP1` rely on, only tracks the handful of leagues in its own `LEAGUE_MAP` (`E0`, `SP1` — Allsvenskan was never in scope). `_apply_cold_start_imputation`'s per-competition group-mean fill (US#134) correctly leaves an all-`NaN` competition column `NaN` rather than borrowing another competition's mean, so every single SWE fixture computes `(74-21)/74 = 71.6%` feature completeness, just under `_cold_start_risk`'s 0.85 threshold (`src/forecast/forecast_service.py`) — permanently, not just for these two matches. | not fixed — display toggle applied instead | The real fix (splitting `XG_`/`LUCK_` into their own feature-group sub-tag, off for SWE, mirroring how `_SHOTS`/`_CORNERS` are already split) was scoped but not applied: given the gap is structural (the underlying xG data doesn't exist anywhere in this app's ingestion, not fetchable), and the big-5 leagues' season was starting the same week, the user asked to stop featuring SWE entirely instead. Addressed via W104 (`documents/app_user_stories.md` Phase 24) — `config/competitions.yaml`'s new `display_enabled: false` on SWE, consulted by `get_fixtures()`/pregeneration and the nightly EOD/T-30 loop. Leaving this bug open (not "won't fix") since the feature-group split is still the correct eventual fix if SWE is ever re-featured — re-enabling today via the same flag would immediately reproduce this exact symptom on every SWE card again. |
| BUG-042 | `run_fetch_fotmob`'s default season range (when `from_season` isn't given explicitly) spanned a league's *entire* ingested history, not a sensible recent window — one real HTTP request per calendar day across a decade-plus, realistically hours per `refresh-data` run | Found live 2026-08-12, direct user question ("why do we need all season data on the backend") that correctly anticipated this exact class of over-fetching, then confirmed live watching SP1's `refresh-data` run: the FotMob step was still iterating daily requests from September 2014 onward, minutes in, with many more years left to go. `detected_from = (from_season or bounds[0]) - 1` derived the start of the fetch window from `MIN(YEAR(date))` across the *entire* `raw_matches` table for that league (its full ingested depth, going back to whenever the competition was first added — for SP1, 2011), not a bounded recent window — for a league with a decade+ of history, `run_fetch_fotmob` (called with no explicit `from_season`/`to_season` by `run_refresh_data`) would iterate roughly 15 seasons × ~300+ days each, at ~1-2s/request, realistically 2+ hours, entirely for enrichment data (FotMob per-player match stats) that isn't even required for a forecast to work at all — confirmed separately the same session: live serving computes its rolling/H2H features from `raw_matches`/`feature_store` directly, with bounded 3-5-match/last-5-meetings windows, independent of this FotMob enrichment layer entirely. | fixed | New module constant `FOTMOB_DEFAULT_SEASON_LOOKBACK = 2` (`main.py`). `detected_from` now defaults to `max(bounds[0] - 1, detected_to - FOTMOB_DEFAULT_SEASON_LOOKBACK + 1)` when `from_season` isn't given — caps the default window to the last 2 seasons, while still never requesting a season entirely before any real match data exists (the `max()` with the original `bounds[0] - 1` floor preserves that). An explicit `--from_season` (a deliberate one-off deep backfill via the CLI) is completely unaffected — opt-in, not silently overridden. New `tests/test_fetch_fotmob_season_range.py` (3 tests, real DuckDB fixtures, all HTTP calls mocked): a decade of real history is capped to exactly 2 seasons by default; a brand-new competition with only one real season doesn't request an earlier, data-free season; an explicit `from_season` still fetches the full unbounded range requested. Red-first verified: temporarily reverted to the old unbounded `(from_season or bounds[0]) - 1` computation, confirmed a real `assert 15 == 2` failure reproducing the exact old over-fetching behavior, reverted back. Full suite: 1044 passed / 1 skipped, zero regressions (up from 1041). |
| BUG-041 | `fetch_finished_match_ids` crashed with an uncaught `AttributeError` on a real 200 OK FotMob response whose JSON body was `null` (or otherwise not a dict), taking out the entire multi-day/multi-season `fetch-fotmob` loop and the whole `refresh-data` chain with it | Found live 2026-08-11/12 during the SP1 `refresh-data` run (right after US#159's DB-lock hardening let it get past the earlier crash) — `run_fetch_fotmob` derives its FotMob date range from `MIN(YEAR(date))` across all of `raw_matches` (the DB's full historical depth for that league, not just the 10 most-recently-scraped seasons), so it genuinely iterates back to 2011 for SP1 -- a date FotMob's matches endpoint apparently has no data for and responds to with a literal `null` body instead of `{"leagues": []}`. `payload.get("leagues", [])` then crashed on `None.get(...)`. The caller's own per-day resilience (`fetch_player_match_stats`'s `try/except requests.RequestException: continue`, meant to skip exactly this kind of single-bad-day failure) didn't catch it, since an `AttributeError` from an unexpectedly-shaped-but-successful response isn't a `requests.RequestException`. | fixed | `fetch_finished_match_ids` now checks `isinstance(payload, dict)` immediately after `resp.json()`; a non-dict payload logs a warning (naming the actual type and date) and returns `[]`, consistent with how the function already treats "no leagues found for this date" and how it already skips a malformed individual match entry with a warning rather than crashing — extending the same "always return a list, only raise for genuine network failures" contract to a whole-payload shape surprise, not just row-level ones. New tests `test_fetch_finished_match_ids_treats_a_null_payload_as_no_matches`/`..._a_list_payload_as_no_matches` (`tests/test_fotmob_fetcher.py`). Red-first verified: temporarily disabled the guard, confirmed both tests fail with the exact real `AttributeError: 'NoneType'/'list' object has no attribute 'get'`, reverted. Full suite: 1041 passed / 1 skipped, zero regressions (up from 1039). |
| BUG-040 | `main.py` unconditionally imports `optuna` at module level (via `src/utils/sweep_runner.py`) for every subcommand, but `optuna` was never declared in `requirements.txt` -- crashed any `python main.py <anything>` invocation in a genuinely fresh environment | Found live 2026-08-11, triggering the first real `refresh-data` run on the Railway deployment (right after BUG-039's fix): `ModuleNotFoundError: No module named 'optuna'` at `main.py` line 45, before `refresh-data`'s own logic ever ran -- every CLI subcommand imports the same top-of-file module graph regardless of which one is invoked, so this would have broken *any* subcommand, not just `refresh-data`. Root cause: `optuna` was installed directly into the local dev venv at some point (for hyperparameter-sweep work, `src/utils/sweep_runner.py`) but never added to `requirements.txt` -- invisible locally because the dev venv already had it, only surfaced in a genuinely fresh install (Railway's), the same structural class of gap as BUG-038 (a real deployment exercises code paths/environments local dev never does). Directly connects to W98 (`requirements.txt` pinning, same session): that pass pinned the *versions* of everything already listed, but never checked for locally-installed packages missing from the list entirely. | fixed | Reproduced deterministically first (not guessed): built a genuinely fresh venv, installed only `requirements.txt`, ran `python -c "import main"` -- reproduced the exact same `ModuleNotFoundError` locally before touching anything. Added `optuna==4.9.0` to `requirements.txt` (version read directly from the local venv via `pip show`, matching W98's own pinning convention) and re-ran the same fresh-venv import check -- succeeds cleanly, confirming `optuna` was the only gap (not a partial fix papering over one of several). Full suite: 1032 passed / 1 skipped, unaffected (this fix is a `requirements.txt`-only change, no application code touched). |
| BUG-039 | `trigger_data_refresh` (W101) redirected its subprocess's stdout/stderr into a pipe nobody ever read -- hid the refresh's output from Railway's logs and risked the refresh hanging forever | Found live 2026-08-10, minutes after W101 first shipped: the user triggered all three leagues' refreshes, then reported seeing no new Deploy Log activity at all and the dashboard's staleness numbers unchanged after several minutes. `subprocess.Popen(..., stdout=subprocess.PIPE, stderr=subprocess.STDOUT)` was set but nothing ever called `.communicate()`/`.read()` on it -- a classic Python subprocess pitfall: (1) redirecting to an unread pipe means the output never reaches the container's own stdout, which is what Railway's Deploy Logs actually capture, so the refresh's real progress was invisible by design, not just slow; (2) once a child process writes enough output to fill the OS pipe buffer (commonly ~64KB on Linux) with nobody draining it, its next `write()` call blocks indefinitely -- `main.py refresh-data`'s own `LOGGER.info` calls across scrape/ingest/understat/fotmob/lineup steps are easily enough output to hit that, meaning the triggered refreshes could have silently hung forever rather than erroring or completing. Consistent with a genuine "Stopping Container"/restart the user's own log screenshot showed shortly after triggering all three concurrently -- plausibly memory pressure from 3 simultaneously-stuck subprocesses (each having imported `main.py`'s own `mlflow`/training-machinery footprint) stacking on top of the base server, though not separately confirmed. | fixed | Removed `stdout=subprocess.PIPE, stderr=subprocess.STDOUT` entirely -- leaving both unset makes the subprocess inherit the parent process's own file descriptors, so its output flows directly into the same stream Railway already captures, with no pipe to fill or deadlock on. New test `test_does_not_redirect_subprocess_output_into_an_unread_pipe` asserts neither kwarg is passed to `Popen`. Red-first verified: reintroduced the pipe redirect, confirmed a real `AssertionError` showing `stdout`/`stderr` present in the call kwargs, reverted. Any subprocesses started under the old, buggy code before this fix landed get cleaned up as a side effect of the redeploy itself (a container restart kills all its child processes) -- no separate manual cleanup needed. Full backend suite: 298 passed, zero regressions. |
| BUG-038 | `PersistingOddsClient.get_odds()` (the real, non-sandbox production odds client) never accepted or forwarded a `date` argument -- crashed with `TypeError` on the very first live (non-sandbox) deploy | Found live 2026-08-09 during the app's first-ever public deployment (Railway), via a real user click on "generate recommendation" and reading the live Railway logs together. `_fetch_odds_for_manual_request` (`app/backend/main.py`, BUG-031's own fix) and `eod_batch.py`'s per-fixture-date odds fetch both call `odds_client.get_odds(sport_key=..., date=...)` -- `OddsAPIClient`/`HistoricalOddsClient` (the two real client classes) both correctly accept `date` (BUG-031/W54/BUG-034), but `PersistingOddsClient` (`app/backend/scheduler_wiring.py`), the thin credit-counter-persisting wrapper `build_odds_client()` actually returns whenever sandbox mode is off, never gained the parameter -- caught by its own broad `except Exception` (degrades to no-odds, not a 500), but silently meant **every manually-triggered recommendation on a live deployment had zero real odds data**, and would have identically broken the scheduled EOD batch once `ENABLE_SCHEDULER` was turned on. Never caught by any test or local/sandbox session before now: local dev always runs with `SANDBOX_MODE=1`, which routes through `HistoricalOddsClient` instead -- `PersistingOddsClient` is structurally only ever exercised by a genuinely live, non-sandbox deployment, which this was the first of. | fixed | `PersistingOddsClient.get_odds()` signature extended to `(self, sport_key: str = "soccer_epl", date: str | None = None)`, forwarded straight through to the wrapped client -- matches both real implementations' signatures exactly, fixing both the manual-regenerate path and the (not-yet-enabled) scheduled EOD batch path from the same root fix. New test `test_persisting_odds_client_forwards_date_to_the_inner_client` (`app/backend/tests/test_scheduler_wiring.py`). Red-first verified: temporarily dropped the `date` forwarding, confirmed the new test fails with a real `AssertionError` showing the missing kwarg, reverted. Full backend suite: 290 passed, zero regressions. |
| BUG-001 | Missing xG and LUCK features blocking training | Feature factory generates xG and LUCK features derived from xG data (luck = fthg - xg_h), but raw_matches table had no xG data initially. When xG is missing, these features become NaN and are stored as string "nan" in DuckDB, causing all training rows to be dropped during dropna(). | fixed | Removed xG_R3/R5, XGA_R3/R5, LUCK_R3/R5 features from schema.yaml selected_features. These will be re-added once Understat integration provides xG data. |
| BUG-002 | Schema.yaml included unsupported features dependent on missing data | Configuration referenced 28 features (xG, XGA, LUCK variants) that cannot be generated without source xG data from ingestion. | fixed | Feature set reduced from 92 available features to 62 data-complete features. Updated config/schema.yaml to remove xG/LUCK-dependent features. |
| BUG-003 | Sklearn mean_squared_error squared parameter compatibility | Regression model evaluation failed: `mean_squared_error(..., squared=False)` threw TypeError about unexpected keyword argument. The sklearn API differs across versions. | fixed | Changed to compute MSE first, then apply np.sqrt() for RMSE. This is compatible across sklearn versions. |
| BUG-004 | Model comparison missed split metrics with suffix-style names | MLflow runs could log metrics as `log_loss_test` while comparison expected `test_log_loss`, causing incomplete comparison reports. | fixed | `ModelComparison.extract_metrics_for_run()` now normalizes both prefix and suffix split metric styles. |
| BUG-005 | XGBoost classifier failed on string multiclass labels | `result_3way` uses labels such as `home`, `draw`, and `away`; XGBoost expected integer class IDs and failed during smoke sweep training. | fixed | `XGBoostModel` now label-encodes classifier labels during training and inverse-transforms predictions. Loss evaluation reads the wrapper class order. |
| BUG-006 | XGBoost multiclass sweep used binary logloss defaults | A multiclass smoke sweep failed because XGBoost received a binary `logloss` evaluation metric for `result_3way`. | fixed | `SweepRunner` now sets task-aware XGBoost objectives/metrics: `multi:softprob`/`mlogloss` for multiclass and `binary:logistic`/`logloss` for binary. |
| BUG-007 | Local MLflow file store has malformed experiment directories | CLI and experiment runs repeatedly warn that `mlruns/1` through several local experiment folders are missing `meta.yaml`. Runs still complete, but logging is noisy and slower. | fixed | Implemented `cleanup-mlflow` CLI command (user story 50) to repair malformed experiments. Successfully recovered 9 malformed experiments by creating minimal meta.yaml files. |
| BUG-010 | League context missing from model_selection.yaml | `config/model_selection.yaml` only contains an `international` context. When `forecast_league` is called, `ForecastService` previously fell back to the international MKT-only models (13 features) but the feature factory generates 142 league features, causing an XGBoost `feature_names mismatch` crash. Partial fix applied: `_load_context_models` now returns empty dict for missing contexts (no glob fallback), so `forecast_league` returns a clean error JSON. | fixed | Full fix (2026-06-27, agent A19): trained all 8 targets with `train-target --model xgb/xgb_regressor --context league` (plain `train-forecast-suite` defaults to `lr`/`rf_regressor`, which crash with "No rows left" since 12 xG/LUCK columns are 100% NaN — XGBoost tolerates NaN natively, non-XGBoost models don't). Found and fixed two latent bugs in the selection pipeline along the way: (1) `run_train_target`/`ModelManager` never tagged `context`/`sweep_stage` on plain (non-sweep) runs, so `select-best-models` could never find them — fixed by tagging both inside `ModelManager.run_pipeline()`. (2) `ModelSelector` constructed `model_path` from the MLflow autolog artifact URI (`.../artifacts/model`, an MLflow-flavor directory), which `ForecastService` can't `joblib.load()` — fixed by logging an `artifact_filename` param and pointing `model_path` at the actual `models/*.joblib` file. `forecast_league` now returns `data_quality.prediction_basis == "team_history_and_market"` for E0 matches, verified via a live spot-check. |
| BUG-008 | Matplotlib/font cache directory is not writable in local environment | Experiment and test commands warn that `/Users/tianqihuang/.matplotlib` and some fontconfig cache directories are not writable, forcing cache rebuilds. | open | Non-fatal. Consider setting a writable `MPLCONFIGDIR` in local dev/test commands. |
| BUG-009 | Over/under 2.5 goals and Asian handicap odds not ingested from source CSVs | Football-data.co.uk CSVs contain over/under 2.5 goals odds (`Avg>2.5`, `Avg<2.5`) and Asian handicap odds (`AHh`, `AvgAHH`, `AvgAHA`) which are the market's direct signal for goals and match outcome. These columns are present in all E0 CSVs but are never stored in `raw_matches` or derived into features. Only `Avg>2.5` is read from the CSV solely for Pydantic validation and then discarded. The market implied probability for over/under 2.5 goals is arguably the single strongest predictor for goals targets and was confirmed absent from feature importance analysis. | fixed | Added `over25_odds`, `under25_odds`, `ah_line`, `ah_home_odds`, `ah_away_odds` columns to `raw_matches`. Extended `data_loader.py` to ingest them. Added `MKT_IMPLIED_OVER25`, `MKT_IMPLIED_UNDER25`, `MKT_AH_LINE`, `MKT_AH_HOME_ODDS`, `MKT_AH_AWAY_ODDS` features in `feature_factory.py`. Updated `config/schema.yaml` selected_features. |
| BUG-011 | SnapshotStore mode/match_id never reached tool-execution thread — record and replay silently behaved as live | `SnapshotStore` (A09) stores `mode`/`match_id`/`match_date` in `threading.local()`. LangGraph's `ToolNode` executes every tool call — even a single one — via `langchain_core.runnables.config.get_executor_for_config()`, which returns a `ContextThreadPoolExecutor`. That executor runs the tool call on a *different* OS thread than the one that called `configure_snapshot_store()`. `ContextThreadPoolExecutor` explicitly copies `contextvars.Context` into its worker (`copy_context().run(...)`), but never touches `threading.local()`, which is strictly per-OS-thread. So the worker thread's `SnapshotStore._local` was always a fresh, uninitialized `threading.local()`, and `mode` silently defaulted to `"live"` regardless of what the calling thread had configured. Effect: `agent-snapshot` ("record" mode) called the real `ForecastService`/Tavily every time but never wrote a snapshot file — every match still got a `_complete.json` marker and printed "OK" because the live call succeeded; `agent-backtest` ("replay" mode) never read a recorded file either, so it silently made fresh live calls and never once raised `SnapshotMissingError`, even though the snapshot corpus on disk was entirely empty (24 directories containing only `_complete.json`, zero tool-response files). This also disabled the `before:<date>` web_search leakage filter (gated on `mode in ("record", "replay")`), since mode always read as `"live"`. Caught because the user manually inspected `data/agent_snapshots/` and found zero tool-call files despite 24/24 "successful" runs — no automated test caught it because all existing `SnapshotStore`/tool tests call `configure_snapshot_store()` and the tool function on the *same* thread, never exercising the real `ToolNode` dispatch path. | fixed | Switched `SnapshotStore` from `threading.local()` to `contextvars.ContextVar` (`src/agent/snapshot_store.py`) — `ContextThreadPoolExecutor` and `asyncio.to_thread` (used by `agent-backtest --concurrency`) both explicitly propagate `contextvars.Context` into worker threads, while a bare `threading.Thread()` (not used anywhere in this codebase) still correctly does *not* inherit it, preserving the cross-match isolation A09/A14 need. Proven via: an isolated repro showing `threading.local()` fails and `contextvars.ContextVar` succeeds across a real `ContextThreadPoolExecutor`; a new regression test `test_mode_and_match_propagate_into_context_thread_pool_executor`; a live single-match `agent-snapshot` run showing real `forecast_league_*.json`/`web_search_*.json` files now appear with `before:2026-03-16` correctly appended to the search query; and a canary-injection test that doctored a recorded `forecast_league` response and confirmed `agent-backtest` replay returned the doctored content rather than a fresh live forecast. Entire A20/A21 pilot corpus and both backtest reports were discarded and re-collected/re-run after the fix — see `agent_techspec.md` Section 18 for the corrected baseline. |
| BUG-013 | Agent can emit `recommendation_type: "direct_bet"` with `current_odds: null` | The LLM's own reasoning occasionally marks a market as a `direct_bet` while `current_odds` is `null` — logically incoherent, since "direct bet now" implies acting on a specific price that in this case doesn't exist (typically because odds were found for other markets in the same match but not this one). First observed during the A21 backtest baseline (`agent_techspec.md` §18.3): this crashed `simulate_flat_stake`/`simulate_kelly_stake` with `TypeError: float() argument must be ... not 'NoneType'`, because `staking.py`'s skip-gate checked `recommendation_type == "direct_bet"` and `correct is not None` but never guarded `current_odds` itself. Only the downstream symptom was patched (both staking functions now also skip when `current_odds is None`, with 2 regression tests in `tests/test_staking.py`) — the root cause, the agent producing this invalid combination in the first place, was never addressed at the prompt/reasoning level. Now directly relevant again: the web app (`documents/app_user_stories.md`) is a second, independent consumer of the same `MatchRecommendation` output (W02's API layer, W16's UI rendering), and will hit the identical invalid combination without its own defensive handling. | fixed | Backtest-staking symptom fixed (2026-06-27, part of A21). Root cause fixed 2026-07-11 as part of `agent_user_stories.md` A28: `extract_recommendation` now downgrades a market's `recommendation_type` from `direct_bet` to `no_bet` at extraction time (the only other value valid for that market-level field) whenever `current_odds` is `null`, appending an explanatory note to top-level `limitations` — the incoherent combination can no longer reach any downstream consumer, staking.py's existing skip-gate included. App-side defensive handling remains separately tracked as `app_user_stories.md` W02/W16, since the app shouldn't assume every historical/cached recommendation generated before this fix also got the benefit of it. |
| BUG-012 | League-context models promoted without `feature_subset` — `forecast_league` fails 100% of the time after US#101-106 feature additions | US#101/#102/#103/#104/#106 added 20 new engineered columns (`SQUAD_*`, `LUCK_HOME/AWAY_BURNOUT_R5`, `XOC_HOME/AWAY`, `FRDS_HOME/AWAY`, `DEF_ANCHOR_HOME/AWAY`) to `config/schema.yaml`'s `selected_features` and to the offline `FeatureFactory.compute_rolling_stats()` pipeline (used to build the batch `feature_store` table), but nobody updated `FeatureFactory.build_for_match()` (`src/features/feature_factory.py:825`) — the on-the-fly feature builder used by the *live* agent inference path (`forecast_league` tool → `ForecastService.forecast_upcoming(match_type="league")`) — to compute those same 20 columns. Separately, `config/model_selection.yaml`'s `international` context entries each carry an explicit `feature_subset` override (13 market-only columns), but **none of the 8 `league` context entries did** — because `ModelSelector` only attached `feature_subset` when the training run happened to log one as an MLflow param, and league training runs never did. `ForecastService.forecast_upcoming`'s league branch also never consulted per-target metadata at all (`feature_names_used = self.feature_names`, unconditionally the full schema list for every target), and a live repro (not just static analysis) showed the actual `KeyError` fired one line earlier than first suspected — at the `feature_count` computation, *before* the existing "align missing columns to NaN" safety net ran — so that safety net could never do its job. A live check of `models/home_goals_xgboostregressor_v1_20260701.joblib.metadata.json` additionally showed the deployed model's own training-time feature list (159 features) already differs from `schema.yaml`'s aspirational list (167 features, including 4 of the 5 lineup-gated families) — `model_selection.yaml`/`ForecastService` never read that per-artifact ground truth at all. Confirmed via 37/37 recorded `forecast_league` snapshots in `data/agent_snapshots/` from the 2026-07-05 live collection run all showing the identical error, with the agent silently recovering by calling `forecast_international` (market-odds-only) instead. No test caught this because `ModelSelector`'s promotion logic (`_is_better()`, `_select_for_target_context()`) only compared MLflow metrics between champion and challenger — it never validated that a promoted model's expected features were computable by the live inference path before writing `model_selection.yaml`. | fixed | Fixed in three layers, each covered by new regression tests (verified live end-to-end against the real `data/fpai_core.db` — `forecast_upcoming` for Everton vs Brighton and 5 other real E0 fixtures now returns `prediction_basis: "team_history_and_market"` with ~94% feature completeness instead of raising): **Layer 1** (`src/features/feature_factory.py`, `build_for_match`) — wired in the same five `_compute_*_features` calls (`_compute_squad_features`, `_compute_luck_burnout_features`, `_compute_xoc_features`, `_compute_frds_features`, `_compute_defensive_anchor_features`) that `compute_rolling_stats()` already used, mirroring the pattern of the five older feature blocks `build_for_match` already reused. Covered by `tests/test_feature_factory.py::test_build_for_match_includes_squad_and_luck_columns`. **Layer 1b/3a/3b** (`src/forecast/forecast_service.py`) — (a) fixed the ordering bug: `feature_count` is now computed *after* the "align missing columns to NaN" loop, not before; (b) `_load_context_models` now reads each model's own `<model_path>.metadata.json` `feature_names` (the training pipeline already writes this file) as the ground truth for what that specific artifact needs, falling back to `model_selection.yaml`'s `feature_subset` override or `schema.yaml`'s full list only when the sidecar is absent; (c) `forecast_upcoming`'s league branch now builds `feature_names_used` as the union of every *loaded* target's own feature list instead of one global `self.feature_names`, and `_predict_target` slices `feature_row` down to each target's own feature list/order before calling `.predict()` — a target is now never asked for a column another target needs but it doesn't. Covered by `tests/test_forecast_league_feature_alignment.py` (two tests: per-model metadata is used over schema.yaml; `forecast_upcoming` succeeds even when schema.yaml lists a column, `XOC_HOME`, that a specific loaded model was never trained on). **Layer 2** (`src/features/feature_factory.py`, `_squad_rolling_from_data` and `_luck_burnout_from_data`) — restructured the join from "exact `match_id` present in `raw_player_match_stats`" (which silently dropped any fixture lacking its own player-stats row — including `build_for_match()`'s synthetic upcoming-match row, and any historical match recorded before FotMob ingestion started — from the rolling window entirely) to "every match in each team's full timeline, home or away, with player stats left-joined and gaps naturally skipped by `rolling().mean()`" — the same append-synthetic-row-then-`shift(1).rolling()` pattern `build_for_match`'s older OFF_/DEF_/DIS_/CTX_ features already used. Covered by `tests/test_squad_features.py::test_squad_rolling_carries_forward_for_match_without_own_player_stats` and `tests/test_luck_burnout.py::test_carries_forward_for_match_without_own_player_stats`; all pre-existing tests in both files still pass unchanged. `_compute_xoc_features`/`_compute_frds_features`/`_compute_defensive_anchor_features` (in `src/features/lineup_features.py`) were deliberately **not** given the same carry-forward treatment — unlike SQUAD_*/LUCK_*, these three need the actual announced starting lineup for *this specific fixture*, and their `fotmob_match_id → raw match_id` resolution depends on player co-occurrence against completed `raw_player_match_stats` rows, so they structurally cannot produce a value for a genuinely unplayed match today regardless of join logic — confirmed live that `match_lineups` doesn't exist yet in `data/fpai_core.db`, so these three legitimately return NaN → cold-start-imputed for every current forecast, which is correct/expected behavior, not a bug to paper over. **Layer 3c/3d** (`src/utils/model_selection.py`, `main.py`) — added `missing_features()` and a promotion-time guard in `ModelSelector._select_for_target_context`: when a `computable_features` set is supplied (wired in `main.py::run_select_best_models` via a live `build_for_match()` sample), a candidate whose required features aren't all computable is refused (logged and skipped) rather than written to `model_selection.yaml`; `feature_subset` is now also backfilled from the model's own `.metadata.json` when the MLflow run didn't log one (previously only `international` runs did). Covered by `tests/test_model_selection.py` (4 tests: `missing_features()` unit tests, promotion refusal on an uncomputable feature, and `feature_subset` backfill). Ran a one-off backfill script to populate `feature_subset` on all 8 existing `league` entries in `config/model_selection.yaml` from their real `.metadata.json` files (a data fix, not a code fix — future `select-best-models` runs do this automatically now). Full test suite (`pytest tests/`, 263 tests excluding the pre-existing, unrelated `test_feature_quality.py` failures) passes with no regressions. **Not in scope / still open separately:** `feature_store` (the offline batch table) itself still lacks `XOC_*`/`FRDS_*`/`DEF_ANCHOR_*` columns because `match_lineups` has never been populated (`fetch-lineups` CLI, US#101, hasn't been run) — this pre-existing gap causes `tests/test_feature_quality.py::test_schema_contract` and 21 other tests in that file to fail both before and after this fix; it's a separate data-ingestion gap, not something this bugfix could or should paper over. The in-progress full-season snapshot collection (PID 2941, checked 2026-07-05) had already finished/stopped by the time this fix landed — any snapshots collected before this fix used the `forecast_international` fallback for every league match and should be re-collected before use in a backtest. |
| BUG-015 | The Odds API and football-data.org spell many clubs differently — `eod_batch.py`'s odds-to-fixture matching silently dropped odds for 6/10 real upcoming fixtures | Discovered live during W25's real-key verification of `odds_api_client.py`. `app/backend/eod_batch.py`'s `odds_lookup()`/`match_odds()` matched a `NormalizedOdds` event to a `NormalizedMatch` fixture by exact, case-insensitive `(home_team, away_team)` string equality — the two providers share no common ID, so name matching was the only join key. A real `GET /v4/sports/soccer_epl/odds` call (2026-07-15) cross-checked against real football-data.org fixtures for the same 10 real upcoming matches (joined by exact kickoff timestamp) found **6 of 10 (60%) with a differently-spelled home and/or away team**: `Man United` vs `Manchester United`, `Nottingham` vs `Nottingham Forest`, `Tottenham` vs `Tottenham Hotspur`, `Man City` vs `Manchester City`, `Brighton Hove` vs `Brighton and Hove Albion`, `Newcastle` vs `Newcastle United`. Effect: for the majority of real fixtures, `run_eod_batch()`/`refresh_match_at_t30()` would silently proceed in no-odds/degraded mode even with real odds genuinely available and a working Odds API key — no error, no log signal a human would notice without specifically comparing both providers' raw names side by side. | fixed | Root cause: `odds_lookup()`/`match_odds()` compared raw provider strings instead of each side's *canonical* team name. Fix: both functions now map both the odds event's and the fixture's team names through the same `TeamNameMapper`/`config/team_mapping.json` that ingestion already uses (`src/ingestion/common/team_mapping.py`) before comparing — the same shared canonical namespace, not raw string equality. Re-running the real cross-check against the fixed `match_odds()` (same real 10-fixture sample) resolved **10/10** fixtures correctly, confirmed via `app/backend/tests/test_eod_batch.py::test_odds_matched_via_canonical_team_name_despite_provider_spelling_differences` (new regression test) and a live re-run of the analysis script against the real captured data. Required adding `"Tottenham Hotspur": "Tottenham"` to `config/team_mapping.json` (the one name genuinely missing from the existing 79-entry mapping) plus an identity entry `"Coventry City": "Coventry City"` (already resolved by coincidence since both providers spell it the same, but was logging spurious "unmapped team" warnings). No other unresolved/unmappable names were found in this real 10-fixture, 20-distinct-club sample — if a future live batch surfaces a name neither provider's mapping covers, `TeamNameMapper` logs a clear "Unmapped team" warning identifying exactly which string needs a new entry. |
| BUG-014 | `international`/`general_purpose` context is completely unloadable — every `model_path` in `model_selection.yaml`'s `contexts.international` block points to a nonexistent file | Discovered live while verifying US#107 (registry-driven league fallback): a call for an unregistered league (e.g. `league="La Liga"`) correctly resolves to `effective_context="international"`, but then raises `FileNotFoundError: No target model artifacts found for context 'international'.` Confirmed this is **not** caused by US#107 — the identical, completely unmodified `forecast_upcoming(..., match_type="international")` call (bypassing the new registry check entirely) fails exactly the same way. Root cause: all 8 targets under `contexts.international` in `config/model_selection.yaml` have `model_path` values that don't exist on disk — 7 are old-style MLflow autolog artifact URIs (`file:/.../mlruns/<experiment>/<run>/artifacts/model`, not a `joblib.load()`-able file — the exact anti-pattern BUG-010 fixed for the `league` context via `ModelSelector`'s `artifact_filename`/real `.joblib` path, but which was apparently never re-run for `international`), and the 8th (`btts`) points to `models/btts_xgboost_v1_20260628.joblib`, a filename that doesn't match either real `btts` artifact actually present in `models/` (`..._20260607.joblib`, `..._20260627.joblib`). Effect: **the entire market-odds-only/`general_purpose` forecast path is broken for every caller today** — any existing `match_type="international"` call (the agent's `forecast_international` tool, any non-E0 match), not just US#107's new fallback path. | fixed | Fixed in two layers inside `src/utils/model_selection.py`'s `ModelSelector`, both TDD'd in `tests/test_bug014_stale_champion_path.py` (4 tests). **Layer 1** (`_select_for_target_context`): `select-best-models` doesn't itself train anything, so re-running it against an already-recorded champion always found the identical run/metric ("no improvement") and returned `None` before ever recomputing `model_path` — permanently preserving a stale/broken path with no way to self-correct. Fix: resolve the current champion's own `model_path` (relative to `model_dir.parent` if not absolute) and only honor "no improvement, skip promotion" when that path actually resolves to a real file; otherwise force-refresh regardless of the metric comparison. This alone fixed 6/8 targets after retraining (`btts`, `result_3way`, `away_goals`, `home_goals`, `total_corners`, `total_goals`). **Layer 2** (`_best_run`, new `_run_artifact_resolves` helper): `away_corners`/`home_corners` still failed after Layer 1 — their retrained (`xgb_regressor`) runs had marginally *worse* metrics than the old broken runs (e.g. 2.1125 vs 2.1121 MAE), so `_best_run` still ranked the broken run "best" among candidates, even though Layer 1 correctly forced a promotion decision. Fix: `_best_run` now filters out any candidate whose own `artifact_filename` doesn't resolve to a real file *before* ranking by metric, so an unloadable run can never be selected as "best" regardless of how competitive its metric looks. Verified end-to-end: re-ran `select-best-models --context international` (per-target, since the two corners targets needed the fix) — all 8/8 `contexts.international` entries in `config/model_selection.yaml` now point to real, `joblib.load()`-able files under `models/`. |
| BUG-016 | `diagnose-model` fails to reload a saved multiclass XGBoost classifier — label-encoding mismatch (`y_true` strings vs. integer `labels`) | Found 2026-07-21 while verifying `diagnose-model --target result_3way --context SWE` end-to-end as part of the Sweden expansion's US#131 (not Sweden-specific — confirmed it also fails on E0's own `result_3way` XGBoost artifacts, just masked there by a separate, unrelated schema-drift error on an older artifact trained before Phase 15's lineup features existed). `load_model_for_diagnostics()` reloads the raw XGBoost booster, but `ModelManager._classification_loss()` then calls `sklearn.metrics.log_loss(y_true, probabilities, labels=labels)` where `y_true` (from `TargetResolver.get_label()`) is the original string labels (`'home'`/`'draw'`/`'away'`) while `labels` appears to come from the reloaded model's `.classes_`, which for a bare reloaded booster (not the training-time `XGBoostModel` wrapper with its label-encoder) resolves to integer classes `[0, 1, 2]` instead — the exact same string-vs-integer class mismatch BUG-005 already fixed for the *training/live-evaluation* path (`XGBoostModel` label-encodes and inverse-transforms), but that fix apparently isn't reconstructed correctly when `diagnose-model` loads a saved artifact back in via `load_model_for_diagnostics()`. Effect: `diagnose-model` currently cannot produce a diagnostics report for any saved multiclass XGBoost classifier, regardless of competition. | open | Not fixed as part of US#131 — confirmed out of scope (pre-existing, not introduced by or specific to the Sweden expansion, and orthogonal to that story's actual per-competition scoping fixes). `diagnose-model`'s missing `--context` argument *was* fixed as part of US#131 (`main.py`/`src/evaluation/diagnostics.py`, threads a `competition_id` through to `ModelManager` so a non-E0 artifact's own feature list/training rows are used instead of always defaulting to E0's) — that fix is confirmed working (Sweden's model no longer hits a feature-list mismatch). This label-encoding bug is what's hit *after* that fix, and needs its own investigation into how `load_model_for_diagnostics()` should reconstruct (or bypass the need for) the training-time label encoder. |
| BUG-018 | `agent-snapshot` intermittently records zero tool-response files for a match while still reporting `OK` and `Errors: 0` | Found 2026-07-23 during `agent_user_stories.md` A36 (collecting a pilot SWE snapshot corpus, 24 matches, `--from-date 2026-07-01 --to-date 2026-07-20 --league SWE`). The CLI's own log reported `[1/24] OK` through `[24/24] OK` and `Done. Processed: 24 | Errors: 0 | Skipped: 0`, but directly inspecting `data/agent_snapshots/` (the same "don't trust the exit code, check the directories" discipline that originally caught BUG-011) found only **10 of 24** match directories with real content (5-6 tool-response files each, `forecast_league_*.json` present); **1** had `_complete.json` with **zero** tool-response files (`Brommapojkarna vs Sirius`, 2026-07-12 — the exact BUG-011 shape); the remaining **13** had **no directory created at all**, despite `run_agent()` returning successfully (no exception) and the CLI printing `OK` for every one of them. `match_id` was confirmed deterministic and unaffected (`generate_match_id()` hashes date/home/away/league — re-querying `raw_matches` right after the run returned the identical match_ids for the same fixtures, ruling out ID drift as an explanation). `SnapshotStore.wrap()` (`src/agent/snapshot_store.py`) only ever writes a file when `self.mode` reads `"record"` — the same class's own docstring documents a prior incident (the one BUG-011 already fixed, switching `threading.local()` to `contextvars.ContextVar`) where the mode contextvar failed to propagate into LangGraph's tool-execution thread, silently falling through to `"live"` mode (`wrapped()`'s `if mode == "live": return fn(**kwargs)` — no interception, no file, no error). The symptom here matches that failure mode exactly, but occurring intermittently *after* the documented fix, not in spite of it never having been applied — suggesting either a different code path than `ContextThreadPoolExecutor`/`asyncio.to_thread` is involved for some fraction of calls, or the propagation gap resurfaces under some condition the original fix didn't cover. **Likely contributing factor, not confirmed as sole cause:** this run shared the machine's single-slot (`-np 1`) local Ollama instance with a separate, unrelated, already-running `agent-snapshot --league E0 --from-date 2025-08-01 --to-date 2026-06-01` process (started before this session, well outside A36's own scope) — every affected match's run completed markedly slower than the unaffected ones, consistent with heavy queueing contention, but contention alone doesn't obviously explain *zero* files rather than merely *slow* file-writing, so this is a hypothesis worth testing (e.g., reproducing under contention without a competing process) rather than a settled root cause. | open | Not investigated further as part of A36 — root-causing a snapshot-recording reliability gap is a distinct concern from Sweden-league integration (the task A36 was scoped under), and doing so properly would need a dedicated repro without competing load on the shared local LLM. **Practical impact on A36:** the resulting 24-match corpus is not usable as a clean pilot baseline — `BacktestHarness`/`agent-backtest` raises `SnapshotMissingError` (by design, no silent skip) for any match without a full recording, so a full-range backtest cannot run over this corpus as collected. The 10 genuinely-complete matches (2026-07-12 to 2026-07-20 sub-range) do independently confirm the SWE agent pipeline runs correctly end-to-end for real, real matches — consistent with A35's mocked regression coverage — but do not constitute the intended pilot baseline. Recommend: (1) re-run `agent-snapshot` for SWE once the local Ollama instance isn't contended by another job, verifying every resulting directory has real tool-response files (not just `_complete.json`) before trusting it, matching A20's own precedent; (2) separately, whoever owns agent-snapshot's reliability should try to reproduce this under contention specifically, to determine whether contextvar propagation is genuinely the mechanism or something else. |
| BUG-022 | `agent-snapshot`/`agent-backtest` share one flat, unpartitioned directory across all leagues — cross-league collisions and a real data-loss incident | Found 2026-07-23 during A34 (E0 full-season recording). `main.py::run_agent_snapshot` and `src/agent/backtest.py::process_match_row` both call `configure_snapshot_store(mode, match_id=...)` with no `base_dir` — every league's recordings land in the same flat `data/agent_snapshots/<match_id>/` directory (`SnapshotStore`'s existing `base_dir` override, built for W37's app-sandbox namespacing, was never used here). `match_id` is a content hash with no league component, so nothing on disk distinguishes an E0 match's snapshot directory from a SWE one. **Concretely caused data loss**: while clearing a stale E0 pilot corpus mid-session (killing and restarting `agent-snapshot` for a model swap), a blanket `rm -rf` of every non-`sandbox` directory in `data/agent_snapshots/` was run without checking for other processes sharing that path — an unrelated, already-running SWE snapshot job (A36) was recording into the exact same directory at the time, and its in-progress matches were very likely deleted along with the intended E0 cleanup. The SWE job continued afterward and regenerated at least 10-11 matches, but the incident is not fully reconstructable. | fixed | Added `league_base_dir(league, base_dir=DEFAULT_BASE_DIR) -> Path` in `src/agent/snapshot_store.py` — normalizes `league` (uppercased, whitespace-stripped, falls back to `"unknown"` if empty/`None`) and returns `base_dir / <LEAGUE>`. `main.py::run_agent_snapshot` and `src/agent/backtest.py::process_match_row` both now compute this per-match and pass it as `configure_snapshot_store(..., base_dir=league_base_dir(row["league"]))`, so recordings land at `data/agent_snapshots/<LEAGUE>/<match_id>/...` — E0 and SWE (and any future league) are now structurally isolated, and a cleanup scoped to one league's subdirectory can never touch another's. The "already complete" marker check/write in `run_agent_snapshot` was updated to the same per-league path. **Not migrated:** the 10-11 leftover flat (pre-fix) SWE directories are left as-is rather than moved into the new `SWE/` subfolder — a future SWE `agent-snapshot` run will simply re-record those specific matches under the new path rather than recognizing them as already-complete; harmless (re-recording, not corruption), just not free. TDD: `tests/test_snapshot_store.py` (new `league_base_dir` tests: known league, `None`, empty string, case normalization) plus updated assertions in `tests/test_backtest.py` and a new test in a `main.py`-covering test file confirming `configure_snapshot_store` receives a league-scoped `base_dir`. |
| BUG-021 | `graph.py` stringifies multi-part `AIMessage.content` with `str()` instead of extracting text, corrupting well-formed JSON from providers that return content as a list of blocks | Found 2026-07-23 while testing the `gemini` provider (`ChatGoogleGenerativeAI` / `langchain-google-genai`) for A34. `agent_node`, `output_node`, and the forced-synthesis path in `src/agent/graph.py` all do `text = last.content if isinstance(last.content, str) else str(last.content)`. This assumes `AIMessage.content` is always a plain string, but `langchain-google-genai` (and other provider integrations that attach per-block metadata, e.g. Gemini's `extras.signature` thought-signature blob) return content as a **list of content-block dicts**: `[{"type": "text", "text": "...", "extras": {"signature": "<huge base64 blob>"}}]`. `str()` on that list renders the *entire Python repr*, including the multi-KB signature blob, as one string. `extract_recommendation()`'s greedy `\{.*\}` regex then matches from the first `{` in that repr (the outer block dict) to the last `}` (somewhere inside the signature blob or the real JSON, whichever comes last) and hands the result to `json_repair`, which "recovers" a structurally-valid-but-garbage dict — observed as 44 Pydantic validation errors, with `markets` split into dozens of single-character/single-token string entries. The actual embedded JSON (visible inside the `'text'` field of the real content block) was well-formed and correct the entire time — this was never a Gemini output-quality problem, purely a content-extraction bug that would affect any provider integration returning multi-part content, not just Gemini. | fixed | Added `_extract_text(content: str \| list) -> str` in `src/agent/graph.py`: returns `content` unchanged if already a string; if a list, concatenates each block's `'text'` value (for dict blocks with `type == "text"`) or the block itself (for bare string entries), ignoring non-text blocks and per-block metadata entirely; falls back to `str(content)` only for a genuinely unrecognized shape. Replaces all three `isinstance(..., str) else str(...)` call sites (`agent_node`'s logging, `output_node`'s primary text extraction, and the forced-synthesis path) with this helper. TDD: `tests/test_agent_graph.py` — new tests cover a plain string (unchanged), a Gemini-shaped list-of-blocks (extracts only the `text` field, ignores `extras`), and a live-shaped regression using the real captured Liverpool/Bournemouth payload structure. |
| BUG-019 | `_format_evidence_message`'s markdown-header formatting causes local LLMs to pattern-complete a narrative report instead of emitting the required JSON recommendation | Found 2026-07-23 during A34's live full-season E0 snapshot recording. Switched from `llama3.1:8b` (52% JSON parse-failure rate, 64/123 matches, `agent_techspec.md` §17's documented weak-model characteristic) to `qwen2.5-coder:7b`, hoping coder-tuning would improve structured-output reliability — measured no improvement on a 4-match spot-check (2/4 failures, the *same* specific matches failing under both models). Strengthened `config/prompts/agent_v1.txt` with an explicit "ABSOLUTE OUTPUT RULE" forbidding prose, plus the forced-synthesis fallback message in `graph.py`'s `output_node` — neither had any measurable effect on the identical failing matches. Inspecting the raw model output showed the failure is not budget exhaustion (`tool_call_count=0`, no `forcing_synthesis_call` in the log — this is the model's organic first response) — the model instead writes a full markdown-formatted "report" (`### Everton vs Aston Villa: Head-to-Head Record`, `#### Overview:`, etc., including fabricated details like attributing Manchester United/Real Madrid players to Everton) instead of the JSON block. Root cause: `_format_evidence_message()` (`src/agent/pipeline.py`) formats the pre-supplied forecast/research evidence using markdown headers (`## ML Forecast`, `## Availability / Injury News`, `## Recent Form Context`) — the model pattern-completes this document structure with its own `###` subsections rather than treating it as inert reference data, an effect strong enough to override explicit contrary instructions placed elsewhere in the system prompt. | fixed | Two-stage iteration, both empirically verified live against `qwen2.5-coder:7b` on the same 4 matches (Liverpool/Bournemouth, Man City/Arsenal, Chelsea/Newcastle, Aston Villa/Everton) at each step. **Stage 1:** removed `##`/`###` markdown headers around the evidence — fixed the "writes a narrative report, no JSON at all" failure, but revealed a second effect: the evidence was still wrapped in a ` ```` ` fenced code block containing a JSON-shaped example (`FORECAST_PAYLOAD: {...}`), and the model started imitating *that* fence's shape (`{"recommendation": "..."}`, single wrapper key) instead of the real schema defined earlier in the prompt — recency bias, the nearest fenced-JSON example wins over an earlier schema definition. **Stage 2:** removed the code fence entirely (plain `UPPERCASE_LABEL:` text, one line each, no JSON-shaped example anywhere near the evidence) and added an explicit reminder of the exact required top-level keys immediately after the evidence, right before the model's own turn. This fixed the wrapper-key problem — the model now reliably produces all 7 correct top-level keys — but exposed a third, deeper issue: the model still doesn't understand `overall` must be one of 4 literal enum strings, and instead populates it with a structured summary object (injury lists, head-to-head stats, match metadata) on 4/4 of the same test matches. This third failure mode was assessed as a model capability ceiling, not a fixable prompt/formatting issue, and iteration stopped there (see A34's completion notes in `agent_user_stories.md` for the full before/after progression and the decision on how to proceed). **What this bug fix did permanently improve, independent of the residual `overall`-type issue:** the model no longer silently produces zero-JSON prose (Stage 1), and no longer wraps its answer in an unpredictable single key instead of the real schema (Stage 2) — both are model-agnostic formatting improvements, not specific to qwen2.5-coder. `tests/test_agent_pipeline.py` extended: `test_format_evidence_message_has_no_markdown_headers`. **Directly caused the discovery of BUG-020** (a real `overall` value shaped like Stage 1/2's intermediate failures crashed `extract_recommendation` outright rather than degrading gracefully). |
| BUG-020 | `extract_recommendation` crashes with an unhandled `TypeError` instead of raising `RecommendationParseError` when the LLM returns a non-string `overall` value | Found 2026-07-23 live during BUG-019's fix verification: a real `qwen2.5-coder:7b` response returned `overall` as a dict (`{"liverpool_form": "mixed", "bournemouth_form": "worse"}`) instead of a string. `src/agent/schema.py::extract_recommendation`'s `if data["overall"] not in _VALID_OVERALL:` check requires hashing the left-hand side for the set-membership test — a dict or list there raises `TypeError: unhashable type: 'dict'`/`'list'` immediately, before the `try/except`-wrapped JSON-decode step and before the Pydantic validation layer (A28) ever runs, so this specific malformed shape completely bypassed A28's entire "malformed output raises RecommendationParseError, never an unhandled exception downstream" guarantee. Crashed the whole `agent-recommend`/`run_agent()` call (`[ERROR] Agent failed: unhashable type: 'dict'`, exit code 1) instead of degrading to `insufficient_data` the way every other malformed-output shape does. Lower severity specifically for `agent-snapshot`'s batch recording (its per-match loop already catches exceptions and logs one `ERROR` line rather than crashing the whole run), but would affect production `agent-recommend` and the app's live recommendation path identically. | fixed | One-line fix in `src/agent/schema.py`: `if not isinstance(data["overall"], str) or data["overall"] not in _VALID_OVERALL:` — type-checks before the membership test, so any non-string `overall` (dict, list, int, None, etc.) now falls through to the same graceful `last_error`/continue path as any other invalid value, uniformly. TDD: `tests/test_agent_schema_validation.py::test_overall_as_unhashable_dict_raises_parse_error_not_type_error` and `test_overall_as_unhashable_list_raises_parse_error_not_type_error`, both failing with the real `TypeError` before the fix; full `tests/test_agent_schema_validation.py` + `tests/test_agent_schema.py` suites (17 tests) pass with zero regressions after. Verified live: the exact real Liverpool/Bournemouth response that originally crashed now exits 0 with a clean `parse=failed` / `insufficient_data` fallback. |
| BUG-023 | Agent hallucinates a completely unrelated "Manchester City vs Liverpool" analysis for real fixtures — confirmed on 5/10 (50%) of a live sandbox precompute batch, with no validation layer catching it | Screenshot report 2026-07-29 traced to the exact command that produced it: `python scripts/launch_sandbox.py 2026-03-08 --precompute`. Queried the live sandbox cache directly (`app/data/sandbox/recommendation_cache.db`) and cross-checked every one of the 10 cached rows against the real fixture data from the running backend's own `/api/fixtures` response. Confirmed the recommendation's own `match` field (and its `explanation`) is **completely disconnected from the real fixture** for 5 of 10: match_id 538076 (real: Crystal Palace v Leeds United) cached `match: {home_team: "Manchester City", away_team: "Liverpool"}`; 538078 (real: Brentford v Wolverhampton) same; 538080 (real: Chelsea v Newcastle) same; 538083 (real: Nottingham v Fulham) same, plus a hardcoded `date: "2023-04-15"`; 538084 (real: West Ham v Man City) same, plus `date: "2023-04-15"` and lowercase `home`/`away` keys instead of `home_team`/`away_team`. Three of the five hallucinated rows also show telltale signs of the LLM reciting a memorized/pretraining example rather than reasoning over the supplied evidence: a fabricated date years off from the real fixture, lowercase schema keys, and market names (`1X2`, `Over/Under`) that don't match this codebase's own `result_3way`/`total_goals` convention used everywhere else. Root cause is upstream of the app: the LLM call inside `run_agent` (`src/agent/graph.py`/`pipeline.py`) isn't reliably grounding its `match`/`explanation` output in the real `match_info` (home_team/away_team/date) it was given — the same failure class BUG-019/BUG-021 already found for this pipeline's evidence formatting, but manifesting here as full hallucinated content rather than a formatting-triggered parse failure. Compounding gap found in `validate_and_degrade` (`app/backend/recommendations.py:267-296`, pre-fix): `match=raw.get("match") or {}` passed the agent's self-reported match field straight through with **no cross-check against the actual requested fixture** — so a hallucinated match/team pairing sailed through the one app-owned validation layer untouched and got cached and served to the frontend as if it were real. | fixed | Fixed the app-side gap (not the LLM's underlying tendency to hallucinate, which is a model-capability issue, not something a prompt/code change can fully guarantee away — same conclusion BUG-019 already reached for this pipeline). `validate_and_degrade` (`app/backend/recommendations.py`) now takes the actually-requested `home_team`/`away_team` and cross-checks them against the agent's self-reported `match` field via two new helpers: `_reported_teams()` (tolerates the `home`/`away` vs `home_team`/`away_team` key variance observed in the real data) and `_teams_match()` (canonical, order-independent comparison using the same `TeamNameMapper`/`config/team_mapping.json` BUG-015 already established, so a plain home/away swap — observed live for match_id 538075, Sunderland/Brighton Hove reported swapped — isn't wrongly flagged, only a genuinely different pair of clubs is). On a mismatch, the recommendation is degraded to `overall="insufficient_data"`, `markets=[]`, with a limitations note naming both the requested and the hallucinated fixture, instead of being served as-is. All 4 real callers (`app/backend/main.py:439`, `app/backend/eod_batch.py:176`, `app/backend/t30_refresh.py:97`, `scripts/sandbox_runbook.py:80`) updated to pass the fixture's real team names. TDD: `app/backend/tests/test_recommendations_schema.py` — 3 new tests (`test_agent_match_mismatch_is_degraded_to_insufficient_data` using the real Brentford/Wolverhampton-vs-hallucinated-Man-City-Liverpool shape, `test_home_away_swap_alone_is_not_a_mismatch`, `test_missing_match_field_is_not_treated_as_a_mismatch`); all 9 tests in the file plus the full `app/backend/tests/` suite (252 tests) pass. Verified against the real data that caused the original report: replayed all 10 real cached payloads from the 2026-03-08 precompute batch through the fixed function — the 5 genuinely hallucinated ones (538076, 538078, 538080, 538083, 538084) now correctly degrade, the other 5 (including the home/away-swapped 538075) pass through unaffected. Also wrote corrected rows into the live sandbox cache for those 5 match_ids so the running sandbox session immediately reflects the fix (confirmed via `GET /api/recommendations/538078` and `.../538084` now returning `insufficient_data` with a clear mismatch explanation instead of the fabricated Man City/Liverpool content). **Follow-up shipped:** the same match-grounding guard is now also enforced one layer upstream, inside `extract_recommendation` itself (`src/agent/schema.py`) — the single choke point every consumer of `run_agent()` (the app, `agent-snapshot`/backtest CLI, `scripts/sandbox_runbook.py`) already funnels through, not just the web app. `reported_teams()`/`teams_match()` (the canonical implementations; `app/backend/recommendations.py`'s `validate_and_degrade` now imports and reuses these instead of keeping its own duplicate copies) are applied per-candidate inside `extract_recommendation`'s existing fenced-JSON-block loop: a candidate naming the wrong pair of clubs is rejected exactly like any other malformed candidate (tries the next fenced block if there is one; otherwise raises the pre-existing `RecommendationParseError`, which `graph.py`'s `_build_recommendation` already turns into a safe `insufficient_data` placeholder keyed on the *real* `match_info`, not the hallucinated one — no new degrade path needed, the existing one was already correct). `home_team`/`away_team` are optional/backward-compatible parameters (every pre-existing caller/test omits them, and the check is skipped when they're absent). Scoped to match `BUG-013`/A29's existing precedent: only applied on the free-text extraction path, not `graph.py`'s `_structured_output()` ollama branch (which already skips those other downgrades too, a pre-existing asymmetry, not something introduced or newly regressed here) — the app-side check remains the layer that protects that path and any pre-existing cached data. TDD: 3 new tests in `tests/test_agent_schema_validation.py` (mismatch raises when teams supplied, check is skipped when they're not, a home/away swap alone is tolerated) and 1 end-to-end test in `tests/test_agent_graph.py` (`test_run_agent_degrades_when_llm_hallucinates_a_different_match`, mocking the LLM to return the real hallucinated Man-City-v-Liverpool shape for a real Brentford-v-Wolverhampton request and asserting `run_agent()` returns `insufficient_data` keyed on the real match_info). Full suite (864 tests) passes. **Not fixed / still explicitly out of scope:** *why* the agent hallucinates toward this specific "Manchester City vs Liverpool" attractor in the first place — that would need prompt/evidence-grounding investigation in `src/agent/pipeline.py`/`graph.py`'s message construction, separate follow-up work only worth pursuing if the hallucination rate remains a problem beyond what these two rule-based guards now contain. **2026-07-31 real-data confirmation:** switching the LLM provider (not a code fix — `scripts/launch_sandbox.py --precompute`/`scripts/scenario_runbook.py` gained a `--config` opt-in, Section "Provider/model" in `agent_techspec.md`) from local `llama3.1:8b` to `config/agent_config_deepseek.yaml` (A42) and re-running the identical 2026-03-08 precompute batch (10 E0 + 10 SWE, real odds, real Tavily searches) produced **0/20 match mismatches** (vs. 5/10 on the local model) and **zero non-canonical market names** (BUG-027) — every recommendation correctly named the real fixture, cited real odds/injury evidence (e.g. Liverpool v Tottenham's BTTS edge math and Tottenham's real injury list checked out exactly against the real 2.20 odds), and used only `result_3way`/`btts`/`total_goals`. Confirms this was substantially a model-capability ceiling, not solely a prompt/pipeline defect — the two rule-based guards remain in place as defense-in-depth regardless of provider. |
| BUG-024 | West Ham v Man City card's 2.10 odds / +7.4% edge are not for West Ham v Man City at all — confirmed fabricated, same root cause as BUG-023 | Follow-up to BUG-023 using the same direct cache query: match_id 538084's cached recommendation (rendered on the Dashboard as the "West Ham v Man City" card) has its own `match` field reading `{"home": "Manchester City", "away": "Liverpool", "date": "2023-04-15", "league": "Premier League"}` — the 2.10 odds/+7.4% edge/0.55 ML probability shown are values the LLM generated for its hallucinated "Manchester City vs Liverpool" scenario, not anything computed for the real West Ham v Man City fixture. That Man City happens to also be the away side in the real fixture is coincidental; the odds/edge numbers carry no real information about this match. | fixed | Same fix as BUG-023 (`validate_and_degrade`'s new match cross-check) — verified directly: `GET /api/recommendations/538084?date=2026-03-14` now returns `overall: "insufficient_data"`, `markets: []`, with the limitations note "Agent output was for Manchester City v Liverpool, not the requested West Ham v Man City -- discarded as a mismatch," instead of the fabricated 2.10/+7.4% figures. |
| BUG-030 | A Tavily `web_search` API failure (quota exceeded, rate limit, network error, ...) raised uncaught all the way to a raw `POST /api/recommendations` 500, instead of degrading like the existing missing-key case | User flagged a live "Could not reach the agent." error on the Dashboard (Brighton Hove v Liverpool). Reproduced live: `POST /api/recommendations` for that fixture returned `500 Internal Server Error`; a direct `recommendations.run_agent()` call surfaced the real traceback — `tavily.errors.ForbiddenError: This request exceeds your plan's set usage limit`, raised from `client.search()` inside `_web_search_impl` (`src/agent/tools.py`), uncaught all the way up through `pipeline.py`'s `research_node` (which calls it unconditionally, with no try/except of its own, for every match) and `create_recommendation` (`app/backend/main.py`, no try/except around its `run_agent` call either). `_web_search_impl` already had graceful degradation for one specific failure mode — no `TAVILY_API_KEY` configured, returning a `TOOL_PERMANENTLY_UNAVAILABLE` sentinel the prompt has an explicit stop-rule for — but nothing covered a real API-level failure with a valid key (quota, rate limit, transient network error), which crashes every single request until the quota resets, not just this one match. | fixed | `_web_search_impl` now wraps `client.search(...)` in `try/except Exception`, returning the same `TOOL_PERMANENTLY_UNAVAILABLE`-style sentinel (with the real error message included) on any failure — reusing the missing-key case's existing prompt-level stop-rule instead of inventing a second failure mode every caller would need to special-case. Verified live: the same real Brighton Hove v Liverpool call now returns `overall: "insufficient_data"` with a clear explanation instead of crashing (the odds-verification search also failing meant no forecast could run either, correctly degrading rather than fabricating a recommendation). TDD: `test_web_search_tavily_failure_degrades_instead_of_raising` (`tests/test_agent_tools_snapshot.py`). Full `tests/` suite: 881 passed / 1 skipped (1 pre-existing, unrelated failure). **Not fixed / explicitly out of scope:** no try/except was added around `run_agent()` itself in `app/backend/main.py`/`recommendations.py` — this fix closes the one concretely reproduced failure mode (Tavily), not every conceivable uncaught exception from the agent graph (e.g. a DeepSeek API outage); a broader server-side safety net wasn't requested and would be speculative without a second reproduced case. |
| BUG-031 | Manual "regenerate now" odds fetch queries the wrong date in sandbox mode — silently finds no odds for any fixture not dated exactly on the sandbox's `as_of`, even when real odds exist | Found live investigating the same Brighton Hove v Liverpool "Insufficient Data" card (kickoff 6 days after sandbox `as_of`): `_fetch_odds_for_manual_request()` (`app/backend/main.py`) called `odds_client.get_odds(sport_key=sport_key)` with no `date` argument. Reproduced directly: without `date`, `HistoricalOddsClient.get_odds()` (sandbox mode) falls back to the sandbox's own `as_of` date and returns *that* day's odds events (Crystal Palace v Leeds, Man United v Aston Villa, ...) — none of which match the requested fixture, so `odds_lookup`/`match_odds` correctly find no pair and the forecast degrades to "no odds available," even though real odds for the actual fixture date (confirmed live: `Brighton 3.1 / 3.5 / 2.2` for 2026-03-21) genuinely exist. This never mattered much before W86 (Dashboard's old same-day-only behavior meant most manually-regenerated fixtures *were* dated `as_of`); W86's "next 10 matches" window made most Dashboard fixtures future-dated, making this the common case rather than an edge case. | fixed | Pass `date=request.date` to `get_odds()` — `HistoricalOddsClient` genuinely overrides `sandbox_date` for that call (already an existing parameter, just never used by this call site); `OddsAPIClient` (live) accepts and ignores it, same interface-parity convention `eod_batch.py`'s own per-fixture-date odds fetch already established. Verified live: the exact failing fixture now resolves real odds and a real `direct_bet` recommendation. TDD: `test_odds_fetch_uses_the_fixtures_own_date_not_the_client_default` (`app/backend/tests/test_recommendations_endpoint.py`); 2 pre-existing tests in the same file updated (`assert_called_once_with` now expects the `date` kwarg too). Full backend suite: 13/13 in that file, no regressions elsewhere. |
| BUG-032 | `min_odds` field required by `MarketRecommendationModel` but real DeepSeek output sometimes omits it on a market — one missing field discarded the entire recommendation, not just that market | Found live via a real precompute run: `output_node \| parse=failed \| ... markets.2.min_odds \| Field required` for a real `btts` market that was otherwise complete and valid. Since `min_odds` had no default, Pydantic validation failed for the *whole* candidate JSON block, and `extract_recommendation` discarded every other market's real data along with it, falling back to a generic "Agent did not produce a parseable recommendation" `insufficient_data` placeholder. `min_odds` is also effectively vestigial now that A52's `target_odds` is the verified, code-computed field the UI actually shows (W84/W87) — there's no remaining reason a missing LLM-supplied value here should sink an entire recommendation. | fixed | Defaulted `min_odds: float = 0.0` on both `MarketRecommendationModel` (`src/agent/schema.py`) and `MarketRecommendationOut` (`app/backend/recommendations.py`, defense-in-depth for any cached row missing the key too). Verified live with the exact reported shape (a `btts` market missing `min_odds`) — now parses cleanly. TDD: `test_missing_min_odds_no_longer_sinks_the_whole_recommendation` (`tests/test_agent_schema_validation.py`), `test_missing_min_odds_defaults_instead_of_dropping_the_market_bug032` (`app/backend/tests/test_recommendations_schema.py`). Full suite: 898 passed / 1 skipped (1 pre-existing, unrelated failure). |
| BUG-033 | `FeatureFactory.build_for_match()`'s synthetic-row `pd.concat` triggers pandas' "empty or all-NA entries" `FutureWarning` on *every* spot-forecast call, not just cold-start ones — real root cause was a dtype mismatch, not emptiness | User flagged the warning in test/log output while investigating an unrelated issue. First fix attempt (skip the concat entirely when `raw_df` is empty — the literal cold-start case) only handled the fully-empty scenario; the warning kept firing for ordinary matches with real history. Root-caused precisely via column-by-column bisection against real data rather than guessing further: DuckDB returns `fthg`/`ftag` as `int32` and most other stat columns as `float32`, while the synthetic row's `np.nan` literals force `float64` for most columns but `over25_odds`/`ah_line`/`ah_home_odds`/`ah_away_odds` (which default to Python `None`, not `np.nan`, when the caller doesn't supply them) infer `object` dtype instead — concatenating a real-dtype column against a same-named column at a *different* numeric (or object) dtype that's entirely NaN in one operand is pandas' actual trigger, confirmed by isolating single-column concats until the exact combination that warned was found. | fixed | Two-sided dtype alignment right before the concat: `raw_df`'s numeric columns upcast to `float64` (the `else` branch, non-empty `raw_df`); `synthetic_df`'s known-numeric columns (including the `None`-defaulted ones) explicitly cast to `float64` right after construction, regardless of which branch runs. Verified with `python -W error::FutureWarning` against both a real-history match (Fulham v Burnley) and a fully cold-start one (two fabricated team names) — neither raises. Full suite: 898 passed / 1 skipped (1 pre-existing, unrelated failure), zero new warnings anywhere in the run. |
| BUG-034 | `HistoricalOddsClient` (the sandbox-mode odds source) accepted `sport_key` for interface parity but silently ignored it — always queried `league='E0'` regardless of what competition was actually requested | Found live wiring La Liga (SP1) into the Dashboard: a `SANDBOX_MODE=1` recommendation request for a real SP1 fixture (Rayo Vallecano v Levante, 2026-03-16) returned real *E0* odds (Brentford v Wolves) instead — `odds_lookup`/`match_odds` correctly found no team-name match against that mismatched data and degraded to "no odds available," even though real SP1 odds genuinely exist in `raw_matches` for that date. Root cause: `get_odds(sport_key=..., date=...)`'s query was hardcoded `WHERE league = '{LEAGUE_CODE}'` ("E0"), with a comment explicitly noting `sport_key` was "unused... since raw_matches is already scoped to a single league" — true when written (pre-Sweden), silently wrong the moment a second competition existed. This was a **latent bug retroactively affecting Sweden too**, never caught because SWE's real *live* (non-sandbox) odds fetching goes through `OddsAPIClient` instead, which does respect `sport_key` correctly — only a sandbox-mode SWE or SP1 request ever exercised this specific client's broken path. Confirmed both `SWE` and `SP1` have complete real odds coverage in `raw_matches` (3,489/3,489 and 3,800/3,800 rows respectively) — the data was never the problem, only the query. | fixed | Added a `sport_key -> league` reverse lookup off the existing `ODDS_SPORT_KEY_BY_COMPETITION` mapping; the query now filters `WHERE league = ?` using the resolved league (falling back to `LEAGUE_CODE`/"E0" for an unrecognized `sport_key`, preserving the exact prior default). Also switched the query from an f-string-interpolated league to a proper bound parameter. Verified live: the same real Rayo Vallecano v Levante request now resolves real SP1 odds (1.7/3.75/5.0, matching `raw_matches` exactly) and generates a real recommendation (degraded to `market_odds_only` since SP1 has no trained `model_selection.yaml` entry yet — expected, matches how Sweden started before its own models existed). TDD: 3 new tests in `app/backend/tests/test_historical_odds_client.py` (a same-date, different-league row must not leak into an E0 query; the default/explicit `soccer_epl` sport_key still scopes to E0 unchanged; an unrecognized sport_key falls back to E0 rather than erroring). Full suite: 976 passed / 1 skipped (1 pre-existing, unrelated failure). |
| BUG-025 | Dashboard's "next matches" fallback may structurally starve Allsvenskan (SWE) fixtures even when the Swedish season is live | Reported live via screenshot 2026-07-29: 2026-07-29 falls in the middle of Allsvenskan's active season (Mar-Nov) but before EPL's 2026-27 season opener, so the dashboard correctly shows "No fixtures today" and falls back to its 90-day-forward "next matches" view (`MatchUI.tsx:665-712`) — which returned 10 fixtures, all Premier League, with no Allsvenskan section at all. Confirmed this fallback does query both leagues (`GET /api/fixtures` queries `FootballDataClient` (E0) and `SwedenFixturesClient` (SWE) unconditionally per `COMPETITION_ALLOWLIST = frozenset({"E0", "SWE"})`, `app/backend/match_info.py:18`; frontend's `groupByLeague` doesn't filter SWE out, `app/frontend/lib/dashboardMetrics.ts:42-63`), and the fallback's own sort-by-kickoff-ascending-then-slice(10) (`MatchUI.tsx:697-702`) would legitimately produce an all-EPL top-10 if SWE's client simply returns fewer/no near-term fixtures relative to E0's. Likely contributing factor: `SwedenFixturesClient.get_fixtures` (`app/backend/sweden_fixtures_client.py:86-99`) sources from The Odds API's `/sports/{sport_key}/events` endpoint, which (unlike `FootballDataClient`'s football-data.org season-schedule source for E0) is known to only list a short near-term window of upcoming events with posted odds — plausibly much shorter than E0's 90-day lookahead, which would explain SWE fixtures vanishing from a wide forward-looking window even mid-season. | open | Not yet confirmed against a real Odds API response for the Allsvenskan sport key on this date — next step is to call `SwedenFixturesClient.get_fixtures` directly for a 90-day window and see how many events it actually returns today, to confirm whether the events endpoint's effective horizon (not a code bug in the query/filter itself) is the cause, or whether something else (e.g. sport_key/competition mismatch) is silently zeroing out SWE. Separate from this specific fallback path: the *same-day* (non-fallback) query at `MatchUI.tsx:654-655` would have the identical characteristic once EPL's season starts and SWE again has near-term fixtures worth surfacing. |
| BUG-029 | 7 of 8 E0 ML forecast targets silently missing from every recommendation — `model_selection.yaml`'s `model_path` pointed at nonexistent MLflow artifact URIs, only `btts` had a real path | User flagged a real "Insufficient Data" card (Burnley v Bournemouth) whose own explanation said "The ML forecast only produced a BTTS probability... and did not provide 3-way result probabilities," correctly pointing out the forecast pipeline is designed to tolerate missing features (NaN-tolerant XGBoost) and so should never legitimately have only one target available. Reproduced live: a direct `ForecastService.forecast_upcoming()` call for the real fixture (real odds 4.1/3.8/1.83) returned only `{"btts": ...}` — `result_3way`, `total_goals`, `home_corners`, `away_corners`, `total_corners`, `home_goals`, `away_goals` were all silently absent, no error. Root cause: `config/model_selection.yaml`'s `contexts.E0` block had `model_path: file:///.../mlruns/<experiment>/<run>/artifacts/model` (an MLflow autolog artifact URI, not a `joblib.load()`-able file) for 7 of 8 targets — confirmed the paths genuinely don't exist on disk. This is the exact same anti-pattern BUG-010/BUG-014/BUG-017 already found and fixed for other contexts (`international`, a filename-collision variant for `SWE`) — regressed here specifically for E0's `league` context, likely from a later retraining pass that didn't go through the promotion-path fix those bugs applied. `ForecastService._load_context_models`'s existing "skip a target with no loadable model" behavior (by design, for a target a competition genuinely doesn't support, e.g. corners for SWE) silently absorbed this infrastructure failure too, with no signal distinguishing "genuinely unsupported" from "broken path." Attempted the established self-healing remediation first (`select-best-models --context E0 --dry-run`, the exact tool BUG-014's fix hardened against this) — it reported **"No eligible runs" for all 8 targets, including the currently-working `btts`**, meaning MLflow's own tracking backend can't currently supply candidates for E0 at all; a separate, deeper, unresolved gap, not something this fix could repair automatically. | fixed | Found real, valid, promoted `.joblib` artifacts already on disk for all 7 broken targets — each broken target's own `previous_model_path` field named a real file, and for the 2 whose `previous_model_path` pointed at a different, unverified batch (`home_goals`, `total_goals`), used the same 2026-06-27 batch as the other 5 instead, after confirming via each `.metadata.json` sidecar that all 8 targets (including the already-working `btts`) share an identical, consistent shape (147 features, `training_cutoff: 2023-04-27`) — deliberately not the newer 2026-07-21 batch, which turned out to be a different, wrong model shape entirely (13 features, market-odds-only, meant for the `international` context, not `league`). Updated `model_path` for `away_corners`/`away_goals`/`home_corners`/`home_goals`/`result_3way`/`total_corners`/`total_goals` to their real `models/*_v1_20260627.joblib` files (surgical text edits, not a full YAML round-trip, to avoid reformatting `btts`'s existing 147-entry `feature_subset` list). Verified live: the same real Burnley v Bournemouth `forecast_upcoming()` call now returns all 8 targets, `feature_count: 138`, `prediction_basis: "team_history_and_market"` (was `"partial"`/1-target before). Full suite (927 tests) passes — `model_selection.yaml` correctness isn't unit-tested directly (it's data, not code), so this was verified via the same live reproduction that found the bug, not a new test. **Not fixed / explicitly flagged as a separate, deeper gap:** *why* `select-best-models` currently finds zero eligible MLflow runs for E0 at all (not investigated — this fix bypassed the automatic selection path entirely by pointing directly at known-good existing artifacts) means the normal self-healing/promotion mechanism this codebase relies on (and that BUG-014 specifically hardened) is not currently functional for E0, and a future retraining pass may not be able to promote a genuinely new champion without that being fixed first. |
| BUG-028 | `GET /api/recommendations/{match_id}` 500s on any pre-existing cached row whose market/selection predates BUG-027's Literal constraints — found live investigating "6/10 dashboard cards show no data" | User reported the Dashboard's 10-fixture E0 fallback view still showing 6 cards with no data after the DeepSeek precompute test. Reproduced live: `GET /api/recommendations/538077?date=2026-03-14` (Arsenal v Everton) returned HTTP 500. Traced to `get_cached_recommendation` (`app/backend/main.py`, pre-fix): `return MatchRecommendationOut.model_validate(entry.recommendation)` — a raw Pydantic validation directly on the cached dict, bypassing `validate_and_degrade` (the one function this codebase already built specifically so "the app should never blindly trust the agent's schema guarantees hold, including for recommendations that predate [a schema change]"). The cached row for 538077 has `markets[0].selection == "Arsenal"` — valid under the old plain-`str` schema this row was written under, but rejected by BUG-027's `Literal["home","draw","away","yes","no","over_2.5","under_2.5"]` constraint added this same session — so `model_validate` raised an uncaught `ValidationError`, a 500 for what should be a plain cache read. **Separately, investigated why the other 6/10 cards show `insufficient_data`** (not itself a bug): all 6 are BUG-023's match-mismatch guard correctly firing on real, currently-active local-model (`llama3.1:8b`) hallucinations — 5 stale from earlier sessions, 1 brand new (Man United v Aston Villa hallucinated as "Liverpool v Chelsea", generated the same day via `manual_regenerate`), proving the underlying hallucination is still live and ongoing, not historical. Also found: the DeepSeek batch run for this same date is completely invisible to the live app, since `RecommendationCache` is keyed by `agent_config_hash`, and every live/interactive endpoint (`GET`/`POST /api/recommendations`) unconditionally computes that hash from `AgentConfig.default()` (still `llama3.1:8b`/ollama) — only `scripts/launch_sandbox.py --precompute`'s own `--config` opt-in (this session's earlier change) can write or read DeepSeek-hash rows; nothing threads a config choice into the live serving path. | fixed | `validate_and_degrade` (`app/backend/recommendations.py`) had `home_team`/`away_team` made optional (`None` default, mirroring `extract_recommendation`'s own already-optional params) — when omitted, the match-mismatch check (which needs a ground-truth fixture to compare against) is skipped, but the per-market validation below it (which is what this endpoint actually needs) still runs. `get_cached_recommendation` now calls `validate_and_degrade(entry.recommendation)` instead of the raw `model_validate()` call. TDD: `test_omitting_home_away_skips_the_mismatch_check_but_still_degrades_malformed_markets` (`app/backend/tests/test_recommendations_schema.py`) and `test_get_degrades_a_legacy_cached_row_instead_of_500ing` (`app/backend/tests/test_recommendation_cache_endpoints.py`, using the exact real `market="1X2"`/`selection="Arsenal"` shape that was crashing live). Verified directly against the real crashing row (538077) — now returns `direct_bet` with the malformed market dropped, `invalid_market_count=1`, no exception. Full suite (925 tests) passes. Deployed live: found the running sandbox backend was an orphaned process from a prior session (state file pointed at a different, already-dead PID from a failed relaunch that couldn't bind port 8000 because the orphan was still holding it) — killed the real orphan directly, cleared the stale state file, relaunched; re-verified all 10 E0 fixtures live, 538077 now returns 200 instead of 500. **Not fixed / separate decision point:** the live app has no way to serve DeepSeek-generated recommendations at all right now — fixing that requires either changing the shared `config/agent_config.yaml` default (affects every entry point) or adding a config-selection mechanism to the live API itself (bigger change, not yet scoped). |
| BUG-026 | Dashboard card's relative-day label ("today"/"in N days") disappears whenever a market recommendation is shown — inconsistent, missing on most cards in the 90-day fallback list | Reported live via screenshot 2026-07-30: on the "next matches" fallback view (spans many different days, W46/W51), 3 of 4 visible cards showed a market/selection line (`Over/Under 2.5 goals · over_2.5`, `result_3way · draw`, `result_3way · Arsenal`) with no day/date indicator anywhere on the card, while the 4th (an Insufficient Data card with no market to show) displayed `in 6 days`. Root cause: `MatchCard`'s bottom-line text (`app/frontend/components/MatchUI.tsx`, then line 559) was `{shown ? \`${shown.market} · ${shown.selection}\` : formatDay(...)}` — the day label only rendered when there was *no* market to display instead, so it silently vanished for any card with an active recommendation, which is most of them on a multi-day fallback list. | fixed | Computed `day = formatDay(...)` once per card and changed the line to `{shown ? \`${shown.market} · ${shown.selection} · ${day}\` : day}` (`MatchUI.tsx`) — the day label now always shows; behavior for the no-market case is byte-for-byte unchanged (verified via the full existing date-boundary test suite passing unmodified). New test `app/frontend/components/__tests__/MatchUI.dateboundary.test.tsx::"still shows the relative-day label even when a market is being displayed..."` covers the previously-untested market-present branch. Full frontend suite (78 tests) passes. |
| BUG-027 | Agent's `market`/`selection` fields are unconstrained free text — the same result_3way market rendered as "1X2" for one real fixture; other generations invented markets/selections entirely outside this app's schema | Investigated after a user question ("1x2 market should be the same as result-3way, why do they have different names?"). Queried the live sandbox cache directly and found the scope is much wider than just "1X2" vs "result_3way": real generated markets included `Asian Handicap`, `Over/Under 2.5`, `Over 1.5 goals`, `Both teams to score`, and even `IF Brommapojkarna to win` (a team-specific string, not a real market type at all); real selections included team names (`Vasteras SK`, `Kalmar`, `AIK`, `Halmstad`) and `home_win` instead of `home`/`draw`/`away`. `config/prompts/agent_v1.txt` already specifies a fixed vocabulary for both fields (`market: result_3way \| btts \| total_goals \| home_corners \| away_corners`, `selection: home \| draw \| away \| yes \| no \| over_2.5 \| under_2.5`) — the same kind of enum already enforced for `overall`/`recommendation_type`/`confidence` elsewhere in this exact schema — but `MarketRecommendationModel.market`/`.selection` (`src/agent/schema.py`) and `MarketRecommendationOut.market`/`.selection` (`app/backend/recommendations.py`) were both plain `str`, so nothing rejected a non-canonical value. | fixed | Constrained both fields to `Literal["result_3way", "btts", "total_goals", "home_corners", "away_corners"]` / `Literal["home", "draw", "away", "yes", "no", "over_2.5", "under_2.5"]` in both Pydantic models — same mechanism already used for the other enum-like fields in this schema, just extended to two fields that had been overlooked. Deliberately rejects rather than tries to alias-map a non-canonical value to its nearest canonical name (e.g. "Over 1.5 goals" → "total_goals"): a market naming a genuinely different betting line can't be safely renamed without misrepresenting which line it actually was, which would be a correctness bug in a betting app, arguably worse than just dropping it — same "drop the malformed market, don't repair it" philosophy this codebase's existing market validation already uses. Effect: `extract_recommendation` (agent layer) now rejects a candidate containing a non-canonical market/selection the same way it already rejects any other malformed candidate (tries the next fenced JSON block, else degrades to `insufficient_data` via the pre-existing `RecommendationParseError` path); `validate_and_degrade` (app layer) drops just that one market and keeps the rest, incrementing `invalid_market_count`, as a defense-in-depth backstop for any recommendation that reaches it without having gone through the agent-side check (e.g. an already-cached row from before this fix). TDD: 2 new tests in `tests/test_agent_schema_validation.py` (non-canonical market/selection each raise `RecommendationParseError`), 1 new test in `app/backend/tests/test_recommendations_schema.py` (a "1X2"-named market is dropped like any other malformed market, the valid one alongside it is kept). Full suite (867 tests) passes. **Not fixed / out of scope:** *why* the local model (`llama3.1:8b` per `config/agent_config.yaml`'s current budget-driven provider choice) doesn't reliably follow the prompt's already-specified vocabulary in the first place — same model-capability-ceiling conclusion as BUG-019/BUG-023, contained by a rule-based guard rather than solved at the source. |
| BUG-017 | Model artifact filenames had no competition_id component — training two competitions' models for the same target/model-type on the same day silently overwrote each other on disk | Found live 2026-07-21 during US#139 (retraining the general-purpose/`international` models on Sweden-pooled data). `ModelManager.run_pipeline()` built artifact filenames as `f"{target_name}_{model_prefix}_v1_{date_tag}.joblib"` — no competition/context component at all. Training all 8 `international`-context targets with XGBoost (US#139) reused the exact same filename pattern as the 5 `SWE`-context targets trained earlier the same day (US#130/131), since target name, model-type prefix, and date all matched across the two competitions. The later (`international`) write silently overwrote all 5 of Sweden's already-committed model files with `international`'s 13-feature MKT-only content — `config/model_selection.yaml`'s `contexts.SWE` entries kept pointing at the now-wrong files (still resolved to *a* real file, so BUG-014's stale-artifact-path existence guard didn't catch it — that guard only checks a path resolves, not that its content matches the expected competition/feature-set). Caught by manually re-verifying `python main.py forecast --league SWE ...` after US#139's retraining and finding the model artifact metadata no longer matched Sweden's expected 74-feature list. | fixed | New `build_artifact_filename(target_name, competition_id, model_prefix, date_tag)` helper (`src/models/model_manager.py`) includes a lowercase competition_id suffix for any competition other than `E0`/`None` (E0 keeps its pre-existing unsuffixed shape, so no already-recorded E0 `model_selection.yaml` entries needed to be re-pointed). `run_pipeline()` now calls this helper instead of building the filename inline. Recovery: retrained Sweden's 5 affected targets again (now saved as `*_swe_*` filenames) and manually re-pointed `config/model_selection.yaml`'s `contexts.SWE` entries to the recovered files (`select-best-models` alone couldn't fix this automatically, since the "current"/corrupted and "best"/recovered candidates had numerically identical metrics — an exact tie the selector correctly, but insufficiently for this case, treats as "no change needed"). Verified live: `forecast --league SWE ...` again returns Sweden's real 5-target payload with the correct 74-feature-derived `feature_completeness`, and `forecast --league E0 ...` remains unaffected. New `tests/test_artifact_filename_collision.py` (4 tests) proves distinct competitions produce distinct filenames on the same day, E0's shape is unchanged, and `competition_id=None` also collapses to the unsuffixed shape. **Not otherwise re-verified:** `international`'s own two promoted artifacts from US#139 (`result_3way`, `home_corners`) were written *after* the collision (so their content is correct) but still carry the old unsuffixed filename shape from before this fix landed — harmless (nothing else collides with them going forward), left as-is rather than renamed, consistent with not touching already-correct E0-shaped artifacts. |
| BUG-035 | `scripts/launch_sandbox.py`'s `--precompute` only queried an exact-date-then-fallback-if-empty window, silently drifting out of sync with W86's Dashboard change (always an unconditional 90-day-forward window) — any league with 1-9 (not exactly 0) real matches on the exact sandbox date left the Dashboard's later-dated "next 10" cards uncovered, stuck on "not yet generated" until individually clicked | Direct user report via screenshot 2026-08-08: several La Liga cards showed "NOT YET GENERATED", and a stale E0 card (Bournemouth v Man United, cached 2026-08-07, predating this session's A52/A54 fixes) never refreshed despite a fresh `--precompute` run. Traced to `fetch_sandbox_fixtures()`/`fetch_sandbox_fixtures_swe()` (`scripts/launch_sandbox.py`): both queried the exact `SANDBOX_DATE` first and only expanded to the 90-day window when that query came back completely empty (W51's original design). W86 later changed `DashboardPage` to always query the wide window unconditionally, regardless of same-day count — its own code comment says as much ("if this window ... ever changes, update that Python copy too, there is no shared implementation") but the Python side was never updated. E0/SP1 both had a handful of real matches on the exact sandbox date, so neither ever triggered the old fallback, silently leaving most of what the Dashboard actually shows uncovered by precompute. | fixed | Both functions now unconditionally query `date_str..date_str+90d`, sort by kickoff, cap at 10 — byte-identical to `DashboardPage`'s own query, no more two-tier exact-then-fallback logic. `used_fallback` return value kept (call-site compatibility) but now always `False`; the precompute status line was simplified to match. Verified live: a full precompute re-run now generates 10/10 for E0, SWE, and SP1 each, covering every fixture the Dashboard's "next 10" actually shows — the previously-stale Bournemouth v Man United and the previously-missing La Liga cards all regenerated under current code. `scripts/test_launch_sandbox.py`'s `TestFetchSandboxFixtures` rewritten for the new unconditional-window contract; `TestPrecomputeRecommendationsFallbackReporting` rewritten to cover a later-dated (window-only-reachable) fixture instead of the now-nonexistent empty-date branch. Full suite: 994 passed / 1 skipped, zero regressions. |
| BUG-036 | `SnapshotStore`'s sandbox-mode replay-first behavior has no staleness detection — a tool response recorded before a real fix (model paths, snapshot content) landed keeps replaying indefinitely afterward, silently serving disproven data forever | Found live twice in the same investigation session (2026-08-08), both via direct user screenshots of cards that should have looked healthy: **(1)** a La Liga (SP1) card (Rayo Vallecano v Levante) showed "Cold start — thin history" mid-season, when every other SP1 fixture correctly used the real trained model — its `forecast_league` snapshot was recorded at 03:11 that morning, before `config/model_selection.yaml`'s SP1 entries were in a consistent state, and kept replaying `model_version: "forecast_suite_international_v1"`/`feature_completeness: 0.31` forever after, even once the real fix was live and every other SP1 match recorded correctly. **(2)** An E0 card (Brentford v Wolverhampton) showed "Insufficient Data" ("the ML forecast only provides BTTS probabilities") — its snapshot was recorded July 31, during the exact window BUG-029 was open (7 of 8 E0 targets silently missing from every forecast). A full scan of `data/agent_snapshots/sandbox/` found **43 E0 match directories** carrying this same pre-BUG-029 single-target signature, spanning 2026-07-25 through 2026-08-07 — every one of them would keep serving the disproven 1-target forecast forever, since nothing about the fix retroactively touches already-recorded snapshot files, and `SnapshotStore` prefers a replay hit over ever calling the live tool again once a match has *any* recording on disk. | fixed (both known instances, and the root structural gap itself — W96) | Both known instances resolved the same way: delete the stale snapshot directory/directories, then re-run `--precompute` so the match(es) record fresh under current code. Verified live for both: the SP1 match now shows `team_history_and_market`/93.9% feature completeness/`cold_start_risk: false`; all 43 E0 matches (the ones still in the Dashboard's current window) regenerated with full 8-target forecasts, and the previously-stuck "Insufficient Data" card now returns a genuine `direct_bet` with a real 9.9% edge. **Structural gap fixed 2026-08-09 (W96, `documents/app_user_stories.md` Phase 22):** both incidents traced to the same concrete cause — `config/model_selection.yaml` changing after a snapshot was recorded — so `app/backend/recommendations.py` now fingerprints that file (SHA-256) into every sandbox `_complete.json` marker at record time, and `_select_sandbox_snapshot_source` only trusts a marker for replay if its stored hash still matches the file's current content; a mismatch (or a pre-fix marker with no hash field at all) falls through to one fresh live re-record instead of replaying forever. Deliberately scoped to just this one file (the only one either real incident implicated, not a general "any code changed" check) and deliberately leaves the separate standalone `agent-snapshot` CLI corpus (A11, historical/backtest determinism) untouched, since that store is meant to stay pinned regardless of later model changes. Self-heals every one of the 43+ already-stale pre-fix markers automatically on next request, without a manual scan-and-purge. |
| BUG-037 | `run_ingest`/`run_fetch_understat`/`run_refresh_sweden_data` silently ignored the `db_manager`/`app_settings` they were called with — always re-derived their own `CSVLoader()`/`FeatureFactory()` from the hardcoded default `"config.yaml"` instead of the caller's actual config | Found while writing US#155's partial-failure recovery test (`documents/user_stories.md`): a test constructing an isolated tmp-path `DuckDBManager`/config and passing it into `main.run_refresh_data()` would have had `run_ingest`'s `CSVLoader()` (and `run_fetch_understat`'s/`run_refresh_sweden_data`'s `FeatureFactory()`) silently bypass that isolation and write into the *real* production `data/fpai_core.db` instead — caught before the test was ever actually run, not after. Harmless in every real CLI invocation to date, since `main.py`'s own dispatch always constructs `app_settings`/`db_manager` from that same default `config.yaml`, so both paths happened to already agree — but a genuine function-signature contract violation (the parameters look respected, they aren't, for this one piece of work each function does) and a real landmine for any future non-default-config usage (tests, multi-environment deployment). | fixed | `DuckDBManager.__init__` (`src/utils/db_manager.py`) now stores `self.config_path`; `run_ingest`, `run_fetch_understat`, and `run_refresh_sweden_data` (`main.py`) now construct `CSVLoader(config_path=db_manager.config_path)`/`FeatureFactory(config_path=db_manager.config_path)` instead of the bare no-arg default. Behavior-preserving for every real caller (both still resolve to the same config.yaml there); only changes behavior for a caller pointing `run_refresh_data` at a genuinely different config. `tests/test_refresh_data_sweden.py`'s `_UnusedDBManager`/`_FakeFactory` test stand-ins updated to accept the now-real `config_path` dependency. Full suite: 997 passed / 1 skipped, zero regressions. Confirmed the real `data/fpai_core.db` was never touched during the investigation (`git status`/file mtime unchanged throughout). |
