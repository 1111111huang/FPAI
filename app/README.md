# FPAI Web App

Third FPAI component: a bettor-facing web app on top of the forecasting engine and betting agent.

Nothing here yet — functionality is still being worked out. See:

- `documents/app_user_stories.md` — open design discussions and stories (start here).
- `documents/app_prd.md` — product requirements (draft).
- `documents/app_techspec.md` — technical specification (draft).

Anticipated structure once implementation starts: `backend/` (FastAPI) and `frontend/` (Next.js).
